# b-104b · 超宽融合指挥总屏 · 剩余执行清单（未做部分）

> 配套总计划 b-104。**本清单只列「尚未动代码」的部分**，逐项可勾、可冻结执行。
> 已完成（不在本清单）：后端三端点 `weather` / `profileRadar` / `dispatch`（commit `c846702`，已上线验证）。
> 状态图例：`[ ]` 未开始 · `[~]` 进行中 · `[fix]` 已写完自检通过（待独立验收 `pass`）。

---

## T1 · 后端补缺（小）

- [ ] **T1.1 预约渠道占比**
  - 取数：`bookingRepository` 新增「今日预约按 `channel` 分组计数」查询（去 CANCELLED）。
  - 出口：`channelService` 加 `todayChannelBreakdown()`，或在 `/api/screen/[metric]` 加 `channels` resolver（返回 `[{code,label,count,percentage}]`，复用 `channelService.list` 的 label/启停）。
  - 约束：PII-free，只出计数/占比。
  - 自检：`/api/screen/channels` 返回真实占比，禁用渠道不计入。

- [ ] **T1.2 POI 高负荷近似**（决策②：近似+真名，不新建模型）
  - 在融合页 RSC 内：`contentRepository.listPois()` 取真实 POI 名；用 `getWeeklyHourlyHeat`/`getHourlyPeak` 当前时段负荷做相对排序，取 Top N。
  - 渲染：POI 名 + 近似负荷条 + 文案标「估算」（不编造精确百分比）。
  - 自检：负荷值来源可追溯，UI 明示「估算」。

## T2 · 可复用组件（先抽组件，后落页面）

- [ ] **T2.1 `ScreenRadar`（新建）**
  - 路径建议：`app/src/lib/ui/charts/ScreenRadar.tsx`（与现有图表组件同层；落位前先 grep 现有图表组件实际目录对齐）。
  - 入参：`axes: {label:string; value:number}[]`（0~100）；纯 SVG 多边形 + 轴线 + 轴标签；走 screen-theme tokens。
  - 自检：5 轴渲染正确，0 值收敛到中心不报错。

- [ ] **T2.2 复用/抽取现有屏组件**
  - 盘点 `src/app/screen/*` 与 `src/lib/ui/*`：折线图、堆叠柱、热力矩阵、漏斗、KPI 卡、承载/进度条、`AmapContainer`、`ScreenCard/Panel`、`PlaceholderTag`。
  - 能复用直接用；需微调的抽公共件，避免每屏各写一套（CLAUDE.md 组件纪律）。
  - 自检：无重复实现；新页面引用公共件。

## T3 · 融合页面本体 `/screen/command`

- [ ] **T3.1 RSC 外壳** `app/src/app/screen/command/page.tsx`
  - 5 列固定栅格 3840×1080，按视口等比缩放容器。
  - 慢变面板 RSC 直读 repo：趋势/客源/分时/热力/画像TOP城市/偏好/漏斗/活动/POI。
  - `export const dynamic = "force-dynamic"`；沿用 `screen/layout.tsx` + `screen-theme.css`。
  - 自检：首屏 SSR 出真实数据，无孤立英文、日期中文格式。

- [ ] **T3.2 顶栏 / Footer**
  - 顶栏：标题 + 实时时钟（client）+ 天气（`/api/screen/weather`，三态：amap/未配置/异常诚实展示，**只天气无 AQI**）。
  - Footer：数据来源 + 导出（`export/[metric]`）/ 刷新。

- [ ] **T3.3 `/screen` index 入口卡**
  - `app/src/app/screen/page.tsx` 顶部加一行/一卡，链向 `/screen/command`（决策①）。
  - 自检：现有 6 分屏入口保留，新卡置顶醒目。

## T4 · 实时联动（client live 岛）

- [ ] **T4.1 live 组件** `app/src/app/screen/command/_command-live.tsx`
  - 轮询易变指标：`occupancy` / `alerts` / `dispatch` / `weather` / `parking`（沿用 15s TTL 端点）。
  - SSE 订阅：`slot_changed` / `checkin_event` / `iot_event` / `parking_state`。
  - 调度面板两态：`dispatch.active` → 特殊态编号策略；否则空态「运行平稳」。
- [ ] **T4.2 红线4 闪红**
  - 在园承载达 90%（`CIRCUIT_BREAK_RATIO`）→ 容量条/态势条闪红告警。
  - 自检：构造 ≥90% 数据可见闪红。

## T5 · 验证

- [ ] **T5.1 静态校验**：`pnpm lint`（含 eslint-boundaries）+ `pnpm exec tsc --noEmit` 全绿。
- [ ] **T5.2 构建**：按编译纪律（先停 dev + `docker compose down`）跑限速 `pnpm build` 通过。
- [ ] **T5.3 CDP 真机核对**：接管 Chrome（`10.7.0.2:9222`），3840 视口打开 `/screen/command` vs 定稿 mockup **逐面板视觉核对** + 截图留档。

## T6 · 上线 & 收尾

- [ ] **T6.1** 推 `deploy-demo` 自动部署，线上实测整屏 + 三端点。
- [ ] **T6.2** 缺口登记 `docs/待办清单.md`：POI 阈值/在园计数真值、AQI 接入、真 AI 调度文案（配 `AI_*` key 后）。
- [ ] **T6.3** 全部 `fix` 后，等独立评审真机验收标 `pass`。

---

## 执行顺序与依赖

```
T1（后端补缺，可与 T2 并行）
        └─► T2（组件）──► T3（页面本体）──► T4（实时）──► T5（验证）──► T6（上线）
```

## 风险（沿用 b-104）
- 中央地图：高德 JS API key 域名白名单 vs demo 纯 IP，交互地图可能受限（服务端 key 不受影响）。
- 示例数据：调度面板当前命中特殊态；清库后可能转空态，演示特殊态需造触发数据。
- build 极吃内存，须按纪律先停 dev/docker 再限速 build。
