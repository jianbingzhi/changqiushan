# b-105 · `deploy-demo` 全量合回 `main` 与单主干收口

> 来源：`ISSUES.md` N06 —— 人工裁决「如果测试通过，都合并到 main」，选定**单主干**，不保留长期部署分支。
> 测试方已做合并勘察并明确：本条是跨模块集成 + CI 变更，不归测试方也不归修复方，交主控立计划。
> 作者：主控（Claude）· 日期：2026 年 7 月 29 日 · 基线 `main` = **`aaaeae1`**、`deploy-demo` = `9befd9c`
> （起草时基线为 `aeb549b`；人工 APPROVE 后 `main` 推进到 `aaaeae1`（r21 复验），合并面已对新基线重验：**仍恰好 2 处 add/add，与原结论一致**。）

---

## 一、范围

**做**：把 `deploy-demo` 的 9 个提交全量合回 `main`（真 merge），并完成随之必须处理的 5 件事——
N08 天气口径收口、根 `PLAN.md` 计划索引、`docs/待办清单.md` §五 由 7 页改 8 页、CI 触发分支改 `main`、`deploy-demo` 分支退役。

**不做**（明确排除，任何 Task / 测试都不得要求）：
- ⛔ `.claude/plan/` 下 20 份历史计划迁移到 `docs/plan/`（`git mv` + 全仓改引用）——已在 `PLAN.md` 以现有路径登记并标「⏳ 待迁移」，**另开任务**。分支集成本身已有 2 处冲突，不叠加 20 文件搬迁。
- ⛔ 根 `AGENTS.md` 正本化（现 `CLAUDE.md` 是 mode `100644` 真文件，非指向 `AGENTS.md` 的 symlink）——与本次集成无关，登记待办。
- ⛔ 漏斗顶层「浏览量」接入（需 C 端埋点）——登记待办，见 D3。
- ⛔ 停掉 `ci-deploy.yml` 的 Vercel 部署——**产品决定，交人工**，见 D7。默认保持原样。

**前置条件（Gate）**：✅ **已满足，可开工**。原要求「等测试方复验完 N12 / N18 / N19 再开工」（因本计划会**换掉 `/screen/command` 整页**，复验途中换页会打断验收）——测试方已于 `aaaeae1`（r21）把三条全部标 `pass`、页面缺陷清零，Gate 解除。

---

## 二、关键决策

| 编号 | 决策 | 理由 |
|---|---|---|
| **D1** | **用真 merge（`git merge --no-ff deploy-demo`），收尾用 `--ff-only`。⛔ 本任务禁用 `merge --squash`** | 已提交的裁决记录里引着 deploy-demo 的 SHA：`docs/issues/round-01-…:299` 引 `c846702`、N06 裁决表引 `9befd9c`/`6c21562`/`2047b91`/`d7099af`。squash / rebase 都会让这些**决策依据变成悬空引用**。真 merge 另有安全网：删分支时 `git branch -d` 能过（git 认得已合并），squash 后该检查永久失效。**这是对协作流程默认 squash 纪律的显式破例**——默认 squash 的前提是「runner 分支 = 一坨线性改动」，分支集成不成立。 |
| **D2** | 两处 add/add 冲突**均取 `deploy-demo` 侧**；但 `b-104` 计划**须手工补回** `main` 侧「一、评审结论」 | `command/page.tsx`：`main` 版与 `6c21562` 版 **md5 逐字节相同**（`b44a40fd…`），`9befd9c` 是其严格重写，取 deploy-demo 侧零损失。`b-104` 计划：deploy-demo 版**只是语义超集**，`main` 版「一、评审结论」的 3 条（计划路径不一致 / 熔断职责边界 / 占位边界必须显式展示）**未逐字出现**（已 grep 核实：deploy-demo 版搜「占位边界」0 命中），直接取会静默丢 3 条评审判断。 |
| **D3** | **接受**定稿版删除「执行边界」卡，不补回 | 该卡的清单 `["待接入","天气/AQI、地域画像、浏览量、三维漫游"]` 已**过期**：天气随本次合并接入；地域画像在定稿页是**真实聚合**（`regionProv`/`regionCity` → 客源地占比 `:261`、TOP8 客源城市 `:343`）；三维漫游根本不在本屏。定稿版 b-104 计划自身的验收口径（`:102`「真实数据或诚实占位/估算标注」）仍由 `园区级估算`(:278)、`暂无点位数据`(:319)、`天气服务暂不可用`(:202) 满足。**唯一仍未接入的「浏览量」**（漏斗顶层，需 C 端埋点）→ 登记 `docs/待办清单.md`，不在本计划实现。 |
| **D4** | N08 修法：类型下沉 `shared/` + 共享 client 组件，两页共用 | `app/eslint.config.mjs:44` 规定 `lib → {lib, shared}`，**`lib` 不能 import `infrastructure`**，且 `:25` `boundaries/ignore` 只豁免 `*.test.ts(x)`，无 type-only 例外。故展示类型必须落 `shared/`（`:40` `infrastructure → shared` 允许、`:44` `lib → shared` 允许）。照抄 `deploy-demo` 自己引入的 `app/src/shared/lib/dispatch.ts` 先例。 |
| **D5** | overview 天气走 **RSC 直读 initial + 客户端低频轮询**，间隔 10 分钟 | 纯 RSC：`/screen/overview` **没有** `ScreenAutoRefresh`（只有 `_overview-occupancy.tsx` 一个轮询岛），挂墙天气会永久冻结在开机那一刻。纯轮询：首屏闪空。二者结合 = `useScreenPoll(metric, initial, ms)` 的既有设计（`use-screen-poll.ts:16`）。间隔取 600s：高德实时天气约 1 小时更新、`fetchWeather` 内 `revalidate: 600`，更快只是空转。 |
| **D6** | AQI **结构性不可得**，口径收窄而非继续挂「待接入」整卡 | 高德 `weatherInfo` 接口本身不返回 AQI（`c846702` commit 已写明），不是"还没接"而是"这个源永远给不了"。故 KpiTile 标题由「实时天气 / 空气质量」收窄为「实时天气」，AQI 降级为 sub 行一个小标注；不再让一张已有真实数据的卡显示 `—`。 |
| **D7** | CI：`deploy-demo-server.yml` 改 `push: branches:[main]` + `paths` 过滤 + 保留 `workflow_dispatch`；`concurrency` 不动 | `paths` 过滤是本项目量身的：该工作流第一步就是 `tar` 打包 `app/` 目录，**部署输入集恰好 = `app/**`**，`docs/`、`prompts/`、`UI/`、`miniprogram/` 的变更对演示机是纯噪声（演示机上跑 `next build`，重建期间站点会抖）。`cancel-in-progress: false` 保持——单主干后 push 变密，部署应排队而非被打断。**Vercel 那条线要不要停 = 产品决定，交人工**（见 §六）。 |
| **D8** | `deploy-demo` 退役 = 打 tag + `-d` 删本地 + 删两个 remote，**且必须排在 main 双推成功、CI 绿之后** | 真 merge 之后删远程分支**不丢任何提交**（9 个 SHA 已在 `main` 一级祖先链上永久可达），不可逆的只有"分支名"这个引用，tag 已补上。⚠️ **硬前置**：`deploy-demo` 正被 checkout 在**另一个 worktree** `/home/agent/projects/Panda/Changqiushan`（`git worktree list` 核实），不切走则 `git branch -d` 报 `used by worktree`。该目录是否有人在用**须人工确认**。 |

---

## 三、合并面（已用 `git merge-tree --write-tree aaaeae1 deploy-demo` 核实，未动任何工作树）

**冲突：恰好 2 个，均 add/add**

| 文件 | 处置 |
|---|---|
| `app/src/app/screen/command/page.tsx` | 取 `deploy-demo`（266 行 → 425 行定稿 5 列） |
| `docs/plan/b-104-超宽融合指挥总屏实施计划.md` | 取 `deploy-demo`，**再手工补回** `main` 版「一、评审结论」（D2） |

**合并新增到 `main` 的 10 个文件**：`.github/workflows/deploy-demo-server.yml`、`app/Caddyfile`、`app/deploy/README.md`、`app/deploy/remote-setup.sh`、`app/docker-compose.demo.yml`、`app/src/app/screen/command/ScreenAutoRefresh.tsx`、`app/src/shared/lib/dispatch.ts`、`docs/plan/b-104b-融合大屏-剩余执行清单.md`、`docs/部署-阿里云ECS自托管.md`、`UI/03_数据大屏/exports/changqiushan-3840-ultrawide-dashboard.html`

**两侧都改过、需要人看的：只有 1 个**

| 文件 | 结论 |
|---|---|
| `app/src/infrastructure/amap/index.ts` | **唯一**两侧都改了逻辑的文件。已核实合并结果**同时保住**：`ROAD_CACHE_SECONDS`(:30)、`resolveFetchedAt`(:38)（N07 修复，仍被 `:96`/`:103` 消费）**与**新增 `WeatherLive`(:122) / `fetchWeather`(:165)。⛔ **绝不可整文件 `--theirs`**——deploy-demo 版没有 N07 修复，覆盖会让 `fetched-at.test.ts` 编译失败 + N07 复发。 |

**已核实为 no-op / 无需复核（澄清测试方与分析中的三处误判）**

- `app/docker-compose.yml`、`app/src/app/screen/page.tsx`、`ScreenShell.tsx`、`use-screen-scale.ts`、`screen-theme.css` —— 两侧 **diff 均为 0 行**，合并零风险。故「compose IPv4-only 会随合并带回」不成立：`main` 已有同一内容，`:202-203` 的 `enable_ipv6` 本就是注释掉的，**不可能带回 IPv6 黑洞版**（与 N09 裁决一致）。
- `app/prisma/seed.ts` —— `deploy-demo` **从未改过**，三点 diff 里不出现，无需复核。
- `app/docker-compose.qa.yml` —— 只在 `main` 上存在，`deploy-demo` 从未创建也未删除 → 合并**不可能删掉它**，正在跑的 QA 栈不受影响。
- 单测文件 —— 合并结果树含 **17 个** `*.test.ts`，与 `main` 的 17 个**完全一致**，无一丢失。
- `main` **已有** `/screen/command`（`8bc3eb0` 在 `main` 祖先链上）且 `screen/page.tsx` 已列 **8** 个入口。故 N06 记录里「main 无此页、导航只有 7 块」**不准确**；`main` 缺的是**定稿 5 列重写**，不是页面本身。真正写错的是 `docs/待办清单.md:139,143`（仍写 C1–C7 / 7 页）。

---

## 四、任务

### T1 · 执行合并（真 merge）

- [x] **T1.1** 基线确认：`git fetch origin jianbingzhi`；确认 `main` = `aaaeae1`（若又已移动，见 T1.7 回退路径）。
- [x] **T1.2** 在 runner 常驻 worktree 的分支上执行 `git merge --no-ff deploy-demo`，停在 2 处 add/add 冲突。
- [x] **T1.3** 解冲突，**用 `git checkout <branch> -- <path>` 而非 `--theirs`**（取分支 tip 内容且自动入暂存区，不会漏 `git add`）：
      ```bash
      git checkout deploy-demo -- app/src/app/screen/command/page.tsx
      git checkout deploy-demo -- "docs/plan/b-104-超宽融合指挥总屏实施计划.md"
      ```
- [x] **T1.4** 手工把 `main` 版 b-104 计划的「一、评审结论」整节以 **`## 附二 · main 侧早期评审结论（8bc3eb0）`** 追加到该文件末尾（D2），`git add`。
- [x] **T1.5** ⛔ **确认 `app/src/infrastructure/amap/index.ts` 未被任何 `--theirs` 波及**：`grep -n "resolveFetchedAt\|ROAD_CACHE_SECONDS\|fetchWeather" app/src/infrastructure/amap/index.ts` 必须**三者全中**。
- [x] **T1.6** 无残留冲突标记：`git diff --check` 且 `git grep -nE '^(<<<<<<<|=======|>>>>>>>)' -- app docs` 无输出。
- [x] **T1.7** `git commit`（**不加 `-m`**，保留默认首行 `Merge branch 'deploy-demo'`；正文写：人工裁决单主干、两处取舍依据、b-104 已补回评审结论）。
      *若 `main` 期间被推进*：⛔ 不要 rebase runner 分支（`--rebase-merges` 会重写 merge commit）；改为先在 runner 分支 `git merge origin/main`，收尾再 `--ff-only`。

**交付物**：runner 分支上一个保留 9 个原始 SHA 的 merge commit。

### T2 · N08 天气口径收口

**接口契约（后续 Task 依赖）**

```ts
// app/src/shared/lib/weather.ts —— 新建。类型下沉至 shared，UI 层才能合法引用（D4）
export interface WeatherLive {
  city: string; weather: string; temperature: number;
  windDirection: string; windPower: string; humidity: number; reportTime: string;
}
export type WeatherResult = { source: "amap" | "unconfigured" | "error"; live: WeatherLive | null };

/** 顶栏一行文案。有数据态返回字符串，其余返回 null（由调用方渲染 PlaceholderTag）。 */
export function formatWeatherHeader(r: WeatherResult): string | null;
/** KPI 卡。value 与 unit 必须分离；不可用态 value="—" 且 unit 为 undefined。 */
export function formatWeatherTile(r: WeatherResult): { value: string; unit?: string; sub: string | null };
/** 三态诚实文案：unconfigured 与 error 必须不同句。 */
export function weatherFallbackText(r: WeatherResult): string;
```

- [x] **T2.1** 新建 `app/src/shared/lib/weather.ts`（上述类型 + 3 个纯函数）。
- [x] **T2.2** 改 `app/src/infrastructure/amap/index.ts`：`WeatherLive` / `WeatherResult` 改为 `import type` 自 `@/shared/lib/weather` 并 `export type` re-export（保持既有引用点不破）；`fetchWeather()` 实现**不动**。
- [x] **T2.3** 新建 `app/src/lib/ui/screen/ScreenWeather.tsx`（`"use client"`，`variant: "header" | "kpi"`），内部 `useScreenPoll<WeatherResult>("weather", initial, 600_000)`。文件名用 PascalCase，与同目录 `KpiTile.tsx` / `ScreenHeader.tsx` / `PlaceholderTag.tsx` 一致。
      > 端点已就绪：合并后 `app/src/app/api/screen/[metric]/route.ts:35` 的 `RESOLVERS` 含 `weather: async () => fetchWeather()`（`:101`），未知 metric `:169` 返回 404。**零后端改动**。
- [x] **T2.4** 改 `app/src/app/screen/overview/page.tsx` 三处：
      - `:38-47` 的 `Promise.all` 追加第 9 项 `fetchWeather().catch(() => ({ source: "error" as const, live: null }))`（与现有 8 项各带 `.catch()` 的写法一致，同 `command/page.tsx:68`）
      - `:103` `rightExtra={<PlaceholderTag text="天气/AQI 待接入" />}` → `rightExtra={<ScreenWeather initial={weather} />}`
      - `:144-149` KpiTile → `<ScreenWeather initial={weather} variant="kpi" />`
- [x] **T2.5** 改合并后的 `app/src/app/screen/command/page.tsx`：内联 `weatherNode`(:196-202) 换成同一个 `ScreenWeather`，消除两页再次漂移的可能。
- [x] **T2.6** 修 `app/src/lib/ui/screen/PlaceholderTag.tsx:2` 的注释「用于天气/AQI、地域、漏斗浏览量、历史回放、地图等未接入区块」——本次后它**两处失实**：① 天气已接入，不再属"未接入区块"；② 「AQI」是孤立英文（红线 6）。改为「用于空气质量、地域、漏斗浏览量、历史回放、地图等未接入区块」。

**命名测试**（新建 `app/src/shared/lib/weather.test.ts`）
> vitest 为 **node 环境、`include: ["src/**/*.test.ts"]` 只收 `.ts`**（`app/vitest.config.ts`），无法直接测 `.tsx`/RSC。故不变量全部锁在纯函数上——这也是 D4 抽 `shared/` 的第二个理由。

- [x] `test_weather_text_no_ascii_letters`：三态全部输出串断言 `not.toMatch(/[A-Za-z]/)`——把红线 6「100% 中文、无孤立英文」变成 CI 卡口，并**永久锁死 AQI 不复活**（`℃`/`%`/`·` 非 ASCII 字母，天然放行）。
- [x] `test_weather_zero_degree_not_swallowed`：`temperature: 0` → header 含「0℃」、tile `value === "0"`。防 `live.temperature && …` / `|| "—"` 这类 falsy 吞值。
- [x] `test_weather_negative_and_zero_humidity`：`temperature: -3` → 「-3℃」；`humidity: 0` → 「湿度 0%」不被吞。
- [x] `test_weather_tile_value_unit_separated`：`.value` **不含**「℃」且 `.unit === "℃"`；不可用态 `.value === "—"` 且 `.unit === undefined`。锁住 KpiTile 契约（`KpiTile.tsx` 对 `value` 施加 `tabular-nums` + 放大 + 辉光，单位混进去必然排版歪）。
- [x] `test_weather_three_states_distinct`：`unconfigured` 与 `error` 文案**必须不同**，且 `{source:"amap", live:null}`（`catch` 分支能造出）走不可用态，不得渲染 `undefined℃` / `NaN`。
- [x] `test_weather_city_fallback`：`city: ""` → 回落「蒲江县」（保住 `command/page.tsx:200` 的 `city || "蒲江县"` 语义）。
- [x] `test_weather_header_and_tile_same_source`：两个格式化函数由**同一入参**派生且温度呈现一致——这条是 **N08 的回归护栏**，它保证的正是「同一数据在两屏不出现两个口径」。

### T3 · CI 触发分支（D7）

- [x] **T3.1** 改 `.github/workflows/deploy-demo-server.yml:17-20`：
      ```yaml
      on:
        push:
          branches: [main]
          paths:
            - 'app/**'
            - '.github/workflows/deploy-demo-server.yml'
        workflow_dispatch:
      ```
      `concurrency: { group: deploy-demo-server, cancel-in-progress: false }` **保持不变**。
- [x] **T3.2** 同轮改掉仍写「推 deploy-demo」的文档，否则就是新的状态漂移：
      `.github/workflows/deploy-demo-server.yml:1-2`（头注释）、`app/deploy/README.md`、
      `docs/plan/b-104-…`、`docs/plan/b-104b-…`（合并后逐处 `grep -rn "deploy-demo" -- docs app/deploy .github` 核对）。
- [x] **T3.3** 补 `app/.env.example`：登记 `AMAP_WEATHER_CITY`（默认蒲江县 `510131`，可 env 覆盖），现未登记、运维不知可改。

### T4 · 文档索引与状态回填（纯文档，按全局规范免代码检查）

- [x] **T4.1** `PLAN.md`（主控已建，含 b-104 / b-105 + 20 份 `.claude/plan/` 历史计划）→ **补登记 b-104b**（随合并才进入 `main`）。
- [x] **T4.2** `docs/待办清单.md`：
      - `:139` 标题 `## 五、数字大屏(C1–C7,挂墙无登录)` → 含 **B104 超宽融合指挥总屏**，共 **8** 块
      - `:143` 「大屏 C1–C7 全量 —— 7 页 + 地基」→ **8 页**，并加 B104 待 QA 条目
      - `:148` 「🆕 天气 / AQI(C5) —— 外部气象 API;现占位标注」→ 天气**已接**高德 `weatherInfo`（随本次合并）；**AQI 仍缺**且**该源结构性不提供**，需另接环境监测源
      - `:21` T9 行同步收窄
      - 新增待办：漏斗顶层「浏览量」（D3）、`.claude/plan/` 20 份迁移、根 `AGENTS.md` 正本化
- [x] **T4.3** `ISSUES.md` 与 `docs/issues/round-01-演示服务器CDP验收.md`：N06 → 已执行（记 merge commit sha）；N08 → **`fix`**。
      ⛔ **实现方只标 `fix`，不得自标 `pass`**——`pass` 是测试方独有的验收权。
- [x] **T4.4** 在 `docs/issues/round-01-…` 追加说明：**N13 / N15 的 `pass` 结论在新版指挥屏上不顺延、须复验**（见 T5.4 的高危项）。
      > ⚠️ **计划原文措辞有误、实现刻意不照抄**：原写「`pass` 结论**已在**新版指挥屏上复验」。执行时新版面根本还没被任何人真机看过，
      > 照字面写就是替测试方伪造了一次验收结论。实际落笔为「**须由测试方复验，通过后才可保留 `pass`**」，
      > 与 §五 风险表「不做 = 悄悄把两个已 pass 的 issue 退回未知状态」的本意一致。**别把文档改回计划字面。**
      > 同轮已在 `ISSUES.md` 的 N13 / N15 两行与 round-01 汇总行补同一条 caveat（索引与正文两处都改）。

### T5 · 验证（按资源纪律排序）

- [x] **T5.1 合并完整性**（零成本，先跑）
      `git diff --check`；`git log --graph --oneline -15` 目视 merge commit 与 9 个 SHA 都在；
      `git branch --merged <runner分支> | grep deploy-demo` **必须有输出**（squash 时此处为空——这是 D1 是否被正确执行的判据）。
- [x] **T5.2 静态闸门**（不需 docker，必跑）
      `pnpm prisma generate` → `pnpm exec tsc --noEmit` → `pnpm lint` → `pnpm test`
      - `tsc`：验 `amap/index.ts` 自动合并的类型一致性 + 425 行定稿页对 `main` 版 `Heatmap724` / `AmapContainer` 的 props 兼容性
      - `lint`：`eslint-boundaries` 验 `ScreenWeather` 没踩 `lib → infrastructure` 卡口；`lint-cn` 验红线 6
      - `test`：`fetched-at.test.ts`（N07）与 `heatmap-option.test.ts`（N13）在合并后仍绿 —— 这是 `amap/index.ts` 自动合并的**唯一硬证据**
- [x] **T5.3 打包**：`pkill -f 'next dev'` → `docker compose down` → **只用 `pnpm build`**（已带限速；⛔ 严禁 `build:fast` / 裸 `next build`）→ 完成后再 `up -d`。
      ⚠️ 本机内存紧张且同机有别项目容器；**本次依赖零变更**（`app/package.json` 与 `pnpm-lock.yaml` 两侧无差异），风险低于常规。是否/何时停 QA 栈打包**须与测试方协调**（跑着的栈归测试独占）。
- [ ] **T5.4 真机验收（测试方，🔴 本次最高危项）**
      `main` 在 round-01 期间**从未改过 `command/page.tsx`**，合并后该页由 266 行整体换成 425 行 5 列新版面。N13/N15 的 `pass` 是在**旧版指挥屏**上验的，**结论不能顺延**：
      - **N13**（热力矩阵空白 + `splitArea` TypeError）与 **N15**（热力 Y 轴周次标签）在新版面上必须复验——定稿页 `:274` 用 `<Heatmap724 matrix height={230} />`，**props 与旧版不同**，能编译 ≠ 渲染对。
      - N13/N15 的修复本体在共享组件（`heatmap-option.ts` / `Heatmap724.tsx`），`main` 侧独有、合并不触碰，故**不会丢修复**；风险仅在新版面喂了不同 props。
      - ❎ **N16 与 N12 不在此列**（已核实）：`status-chip` 在 `app/src/app/screen/` 下**零引用**（纯后台问题）；N12 是 `/traffic/parking` 后台路由，且仍是 `fix` 未 `pass`。
      - N08 收口判据：`/screen/overview` 与 `/screen/command` 顶栏天气**逐字同一口径**；overview KPI 卡出真实温度 + `℃`，sub 只剩「AQI 待接入」小标注。
      - 三态造法：改 `app/.env` 的 `AMAP_KEY` 为非法值 → `error`；删掉整行 → `unconfigured`（会同时打掉 `/traffic/road`，验完恢复）。
        ⚠️ **改 env 后必须重启应用容器**——`force-dynamic` 只让页面重渲染，**不绕过 `fetch` 的 600s Data Cache**，光刷页面可能仍显示旧的成功结果。这一条不交代，验收方极可能误判成"改了没生效"。
      - ⚠️ 本机**永远不要**带 `-f docker-compose.demo.yml`：它 bind `80:80` / `443:443`，同机已有别的 Caddy，QA 栈是靠 `docker-compose.qa.yml` 避开端口冲突才跑起来的。

> **执行留痕（T5.3，2026 年 7 月 29 日）**：`pnpm build` **通过**（编译 28.9s + TypeScript 14.7s + 全路由页面数据收集，
> `/screen/overview` 与 `/screen/command` 均为 ƒ Dynamic）。**但未按计划字面停 docker**——`changqiushan-*` QA 栈是
> **测试方独占的跑着的栈**，开发方 `docker compose down` 会当场中断别人的验收（协作纪律硬红线），故本次
> **只用限速 `pnpm build`、不碰任何容器**；实测 `next dev` 本就没在跑，无需 `pkill`。计划 T5.3 自身也写明
> 「是否/何时停 QA 栈打包**须与测试方协调**」，本次选择不打扰。依赖零变更，实测限速打包与 QA 栈共存未假死。
> 另：runner worktree 无 `app/.env`（gitignore 不随 worktree 传播），首次打包在「收集页面数据」阶段报
> `DATABASE_URL is not set`；已在本 worktree 建一份**仅供打包**的 `.env`（指向不存在的库 `127.0.0.1:5999`，
> 不连任何真实数据库、更不碰 QA 栈；文件本身被 gitignore，不进提交）。**这不是代码缺陷。**

### T6 · 收尾与分支退役（顺序不可颠倒）

- [ ] **T6.1** T5.1/T5.2 全绿 + 文档回填完成后，收尾合回：`git merge --ff-only <runner分支>`。⛔ **禁用 `--squash`**（会把刚保住的 merge commit 与 9 个 SHA 在最后一步全部压碎）。
- [ ] **T6.2** **双推**：`git push origin main && git push jianbingzhi main`（裸 `push` 只到 `origin`，必须显式补推）。
- [ ] **T6.3** 等 GitHub Actions 绿（此时才第一次真正验证 T3 的触发改动）。
- [ ] **T6.4** ⏸ **分支退役需人工确认后再做**（D8）：确认 `/home/agent/projects/Panda/Changqiushan` 那个 worktree 无人在用 → 切走 → `git tag deploy-demo-retired-20260729 9befd9c` → `git branch -d deploy-demo`（`-d` 不是 `-D`）→ 删两个 remote 的分支 → 双推 tag。

---

## 五、风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| `/screen/command` 换版使 N13/N15 的 `pass` 结论失效 | 🔴 高 | T5.4 专项复验并在 issue 文档留痕（T4.4）。不做 = 悄悄把两个已 pass 的 issue 退回未知状态 |
| `amap/index.ts` 被误整文件取 deploy-demo → N07 复发 + `fetched-at.test.ts` 编译失败 | 🟡 中 | T1.5 三关键字必中 + T5.2 `pnpm test` 兜底 |
| b-104 计划丢掉 `main` 侧 3 条评审结论 | 🟡 中 | T1.4 以「附二」追加 |
| 收尾误用 `--squash`，前功尽弃且 9 个提交仅存本地 reflog（90 天后 gc 即真丢） | 🟡 中 | T5.1 的 `git branch --merged` 判据 + T6.1 显式禁令 |
| 本机内存紧张，`pnpm build` 假死 | 🟡 中 | T5.3 固定顺序 + 限速；依赖零变更降低风险；与测试方协调停栈 |
| 改 CI 触发后纯文档提交触发远端全量重建 | 🟢 低 | D7 的 `paths` 过滤（部署输入集恰好 = `app/**`） |
| 删 `deploy-demo` 丢提交 | 🟢 低 | 真 merge 后不成立（9 SHA 在 `main` 祖先链）+ 退役 tag 兜底 + T6 顺序纪律 |

---

## 六、需人工在审批门一并裁决的两条

1. **Vercel 部署线要不要停？** `docs/待办清单.md` 已书面判定「Vercel/Supabase 海外、无备案、微信支付回调收不到，仅适合临时演示」，最终生产走阿里云。现演示服务器（自托管 + Caddy）已承担演示职责，两条线方向相反。
   提案：`ci-deploy.yml` 的 **ci job（lint/tsc/test）保留不动**，**deploy job（Vercel + Supabase migrate）改为仅 `workflow_dispatch` 或整段删除**。
   **默认不动**——它有 secret guard，未配 `VERCEL_*` 时自动 skip（灰色非红），放着无害。
   > 这条命中「PRD 时刻」：属部署形态的硬约束。若采纳，建议一并进 `docs/PRD.md` 的决策记录（一行 + 指针），细则留在待办清单。
2. **`deploy-demo` 分支删除时机**：需确认 worktree `/home/agent/projects/Panda/Changqiushan` 无人在用（D8 硬前置）。

---

## 七、战术手册引用

| 条目 | 是否适用 | 说明 |
|---|---|---|
| `docker-env-file` | ✅ **适用，已采纳** | 本次合并把 `app/deploy/remote-setup.sh` 带入 `main`，它 `cat > .env` 渲染演示机配置（`:70-100`），且该文件**被两种解析器读**：`docker compose` 的 `env_file`（剥引号）与 shell（`:105` `grep '^GOTRUE_JWT_SECRET=' .env \| cut -d'"' -f2` —— **反过来依赖引号存在**）。手册的核心不变量「同一份 `.env` 不被两种解析器读；做不到时按最保守的来」在此**被违反但当前无害**（值全为 hex，无 `#`、无空格）。**本计划不改它**（超出范围），但 T3 后首次 main 触发部署时应跑一次手册的静态自检：`grep -nE '^[A-Za-z_][A-Za-z0-9_]*=.*[^\\]\s#' .env`。该耦合点登记待办。 |
| `pg-job-queue` | ❌ 不适用 | 本计划不引入异步任务 / worker / 队列。 |
| `s3-direct-upload` | ❌ 不适用 | 本计划不涉及上传链路；MinIO/S3 配置随合并原样带入，无改动。 |

---

## 八、评审意见 / 修订索引

| 轮次 | 日期 | 角色 | 类型 | 结论 | sha | 关键发现 / 指针 |
|---|---|---|---|---|---|---|
| r7 | 2026-07-29 | 评审 | code review(执行 diff) | **clean(可收尾 T6.1–T6.3)** | `618322e` | 独立核验全部通过:**T1** merge commit `2779552` 存在、5 个被裁决记录引用的 SHA 逐一 `merge-base --is-ancestor` 验过均在祖先链、`git branch --merged` 有 deploy-demo(D1 判据)、无冲突标记(仅 deploy-demo 侧静态导出 HTML 自带尾随空白,无害);`amap/index.ts` 三关键字 6 命中。**T1.4+**:b-104「附二」不仅补回 3 条评审结论,还含执行方自查出的 §四 验收清单 2 条(letterbox/「余额」),grep 证据(0 命中)俱在——D2 前提不完整被执行方自己补上,加分项。**T2**:`shared/lib/weather.ts` 契约与计划一致,`displayable()` 三层防线(source/live 空/NaN/空 weather)合理;overview 轮询收进 `ScreenWeatherProvider` 单点(屏内口径漂移修复核实);command 无 Provider 但有整页 `ScreenAutoRefresh`(15s refresh + 600s Data Cache)——回落 initial 不会冻结,接线成立。**T3**:CI 触发 main + paths 过滤属实;`AMAP_WEATHER_CITY` compose `environment` 白名单已补(`:44` 默认 510131),r6 抓出的「.env 只插值不注入容器」假完工修复核实。**自检修复抽验**:`numOrNaN` + weather-parse 5 条(0℃/负温/缺失)、lint-cn 正则收窄为 `(?<![=>])>(?!=)`(Fragment 保护注释在,fuzz 声称与 diff 一致)。**四处判断裁定**:①T5.3 不停 QA 栈**正确**——跑着的栈归测试独占,限速 build 已过,计划自身要求协调;②计划外 2 处(lint-cn 修误报未放宽、TOP 8 中文化)属合并集成成本,**认可**;③T4.4 拒绝预写「已复验」**强烈认可**——照计划字面写等于替测试方伪造验收结论,计划措辞缺陷成立,不得改回;④ASCII_ALLOW 产品口径与 WECHAT_*/AI_* 白名单缺失两条**不擅改、登记待办交人工,处置正确**。`pnpm test` 428 passed(19 files)复跑属实;文档只标 fix 合规。⚠️ 收尾注意:main 上有本条 r7 评审 commit,T6.1 前先在 runner 分支 `git merge main` 再 `--ff-only`(即计划 T1.7 既有回退路径) |
| r6 | 2026-07-29 | 执行 | `/code-review` 自检(第二轮,对抗式复验 r5 的修复) | **再修 1 条 HIGH,复跑全绿** | — | 第二轮专门去找「r5 的修复本身有没有引入新缺陷」(r5 已抓到过一次这种事:lint 正则「修误报」反而放宽了红线)。**抓到 1 条 HIGH,是我 r5 ⑨ 的假完工**:`AMAP_WEATHER_CITY` 的链路我只追到「写进服务器 `.env` 文件」就宣布补全,**没追到容器进程**。`app/docker-compose.yml` 的 `app` 服务 `environment:` 块是**显式白名单**,compose 的 `.env` 只用来插值 YAML 里出现的 `${VAR}`、**不会**把 `.env` 的每个 key 自动注进容器 —— 少这一行,`remote-setup.sh` 写的值永远到不了 `process.env`,代码恒取缺省 510131,而 `docs/待办清单.md` 已写成「可经 env 覆盖」的既成能力。已在 compose 补 `AMAP_WEATHER_CITY: ${AMAP_WEATHER_CITY:-510131}` 并**实测** `docker compose config`:缺省出 `510131`、`AMAP_WEATHER_CITY=510100` 时出 `510100`,链路端到端打通。**顺带查出一条既有缺陷,本轮明确不修**:同一个白名单还漏了 `WECHAT_APPID`/`WECHAT_SECRET`/`AI_BASE_URL`/`AI_MODEL`/`AI_API_KEY` —— `remote-setup.sh` 写了、`infrastructure/config/integration.ts` 也在读,但 compose 没透传,**微信支付与 AI 功能在 docker 演示路径下大概率从未生效过**且静默失败(`?? ""` 兜底)。不在 §一 范围、且牵动支付/AI 凭据链路改了要重新验收,已连诊断与改法一并登记 `docs/待办清单.md`,交人工决定是否单独立项。**r5 的 7 项修复经独立对抗复验全部确认无回归**:lint 正则(逐例 fuzz:`>=`/`>>`/`=>`/泛型 `Promise<T>` 不误报、Fragment 恢复检出)、`numOrNaN`(所有消费方都经 `Number.isFinite` 把关;JSON 往返把 NaN 变 null 仍 fail-safe)、Provider 的 `?? initial`(`WeatherResult` 恒为对象,只有真无 Provider 才回落)、`ScreenShell` 本就是 `'use client'` 收 server children,再套一层 client Provider 是标准 RSC 写法、无水合问题、overview 重排缩进 JSX 平衡、`displayable` 拒空 `weather` 不误伤、`weather-parse.test.ts` 的 stub 在 `afterEach` 全部还原、无跨文件污染或顺序依赖。复跑:`tsc --noEmit` 干净 / `lint` 0 error / `pnpm test` **428 passed(19 files)** / `git branch --merged` 判据仍通过 |
| r5 | 2026-07-29 | 执行 | 自检(multi-execute 双审 + `/code-review`) | **据自检修 11 条后全绿** | — | 两名独立审查各自复跑了全部闸门。**修掉的真缺陷**:① 🟠 `amap/index.ts` 的 `Number(live.temperature ?? 0)` —— `Number("")===0`,上游漏传气温会让挂墙大屏在七月**理直气壮显示「0℃」**、与真实读数不可区分(比诚实的「—」更糟),改 `numOrNaN()` 缺值一律 NaN;新增 `weather-parse.test.ts` 5 条,**做过变异验证**(退回 `?? 0` 当场 3 条转红)。② 🟠 我自己在 T5.2 为消 lint 误报改的 `TEXT_NODE_RE` 把 `<` 也放进负向后顾,**误伤 React Fragment 简写 `<>文本</>`**(src/app 下已有 24 处),等于给红线扫描永久开了个口子;收窄为 `(?<![=>])>(?!=)`,已逐例验证:Fragment 恢复检出、`>=`/`>>`/`=>` 仍不误报。③ 🟠 overview 上两个 `ScreenWeather` 各自 `useScreenPoll`,网络抖动时**同一块屏能显示两个温度**——N08 要消灭的「同一数据两个口径」从跨屏挪到了屏内;轮询收进 `ScreenWeatherProvider` 只跑一次。④ 🟡 `displayable()` 未校验 `live.weather`,空串渲染出「蒲江县 ·  12℃」双空格残句。⑤ 🟡 「气象服务未配置密钥」在**无登录、会被拍照转播**的挂墙屏上自曝部署状态,改「天气服务暂未配置」。⑥ 🟡 **D2 的前提不完整**:原只 grep 了「一、评审结论」,复核发现 main 版 **§四 验收清单**另有 2 条(letterbox 缩放无滚动条 / 票务语义含「**余额**」)在 deploy-demo 版逐字不存在——T1.4 按字面执行得没错,它要防的伤害却在隔壁一节发生了;已一并补进「附二」。其中「余额」**比 `lint-cn.mjs` 的 RED_LIST 更宽,自动扫描抓不到**,只能人眼把关。⑦ 🟡 `ISSUES.md`(索引,最先被翻的地方)的 N13/N15 两行与 round-01 汇总行仍写着裸 `pass`,换版待复验的 caveat 只写在了正文——正是全局「索引与正文两处都要改」那条;已补。⑧ 🟡 T5.1/T5.2 复选框漏勾(替换模式没匹配上标题带说明的写法)。⑨ 🔵 T3.3 只在 `.env.example` 登记了 `AMAP_WEATHER_CITY`,CI 与 `remote-setup.sh` **没有透传**,演示机上「运维可经 env 覆盖」实为空话;已补全链路(workflow env → REMOTE_ENV → 服务器 `.env`,默认 510131)。⑩ 🔵 KPI 卡 sub 收窄为只剩「空气质量待接入」(与 T5.4 验收口径一致,且顶栏已完整呈现城市/现象/湿度,不再重复)。⑪ 🔵 补 `aria-live`;修 `PlaceholderTag` 注释。**两条不由执行方单方裁定、留给评审/人工**:(a) `lint-cn.mjs` 的 `ASCII_ALLOW` 仍白名单了 `AQI`/`AI`/`OTA`,故 T2.6 的红线 6 论证其实**严于工具实际口径**,且合并进来的指挥屏有「智能调度策略建议 (AI生成)」与「OTA 渠道」两处可见英文——改词是产品口径决定,已登记待办不擅改;(b) `useScreenPoll` 丢弃 `state`,`SCREEN_TOKEN` 启用后轮询 401 会让大屏**无声冻结**,接 `ScreenHeader.dataSource` 超出本计划范围,已登记待办。复跑:`tsc --noEmit` 干净 / `lint` 0 error / `pnpm test` **428 passed(19 files)** |
| r4 | 2026-07-29 | 执行 | 执行计划 | **T1~T5 完成，只标 `fix`** | `2779552`(merge) | **T1 真 merge**：`git merge --no-ff deploy-demo`，9 个原始 SHA 全在 `main` 祖先链上；`git branch --merged main-runner_A | grep deploy-demo` **有输出**（D1 判据通过，未用 squash）。冲突恰好 2 处 add/add，与 r1/r2/r3 勘察一致，均取 deploy-demo；`b-104` 已以「附二」补回 main 侧 3 条评审结论。**T1.5 三关键字全中**，`amap/index.ts` 走三方自动合并，N07 修复与 `fetchWeather` 并存。**T2 N08 收口**：`shared/lib/weather.ts` + `ScreenWeather` 两屏共用；7 条单测 TDD 先 RED 后 GREEN。**T3 CI** 触发改 `main` + `paths`，运维文档同轮更正。**T4** 索引/状态全部回填。**T5.2** `tsc --noEmit` 干净 / `lint` 0 error / `pnpm test` **420 passed**(18 files) / **T5.3** `pnpm build` 通过。**2 处计划外的必要改动**（合并集成成本，非扩范围）：① `scripts/lint-cn.mjs` 的 `TEXT_NODE_RE` 排除比较符 `>=`/`<=`/`!=`——否则 deploy-demo 侧纯 TS 的 `if (r.dow >= 0 && r.dow < 7)` 被误判成 JSX 文本节点报「孤立英文 dow/hour」（**修误报，未放宽规则**）；② `command/page.tsx` 卡片标题「TOP 8 核心客源城市」真违反红线 6，改「核心客源城市前 8 位」。**T5.3 边界偏离**：未停测试方的 QA 栈打包（见 T6 上方执行留痕）。**T6.4 按指令停在人工确认前，未自行删除 `deploy-demo`** |
| r3 | 2026-07-29 | 主控 | 基线更新 | **人工 APPROVE，已派发执行** | `aaaeae1` | 人工审批通过（`refs.approves=996ada90`，sha·标题对号无误，派发的就是 b-105）。**两处现状更正**：①基线 `main` 由 `aeb549b` 推进到 `aaaeae1`（测试方 r21：N12/N18/N19 全部 `pass`、页面缺陷清零）——计划正文 §头/§一 Gate/§三/T1.1 四处基线已同步，§八 r1/r2 属历史留痕不改；②**Gate 已满足并解除**，可开工。合并面对新基线 `aaaeae1` 重验：**仍恰好 2 处 add/add，与 r1/r2 结论一致**。计划 commit `0581f35` 经查已随测试方 r21 推送到 origin + jianbingzhi 两个 remote（`merge-base --is-ancestor` 双验），无需另行 push。§六 两条人工裁决项人工未答复：Vercel 线按计划**默认不动**；**T6.4 删 `deploy-demo` 分支仍为待人工确认状态**（worktree 占用未确认），执行方须停在该步报人工，不得自行删除。 |
| r2 | 2026-07-29 | 评审 | 计划评审 | **approve(送人工审批门)** | — | 16 项关键事实**逐条独立复核全部属实**:①PLAN.md 已登记 b-104/b-105(含 b-104b 待补与 .claude/plan 迁移另开任务注记);②N06 人工裁决「单主干」有据(ISSUES.md:20 + round-01:322);③`git merge-tree --write-tree aeb549b deploy-demo` 复跑=恰好 2 处 add/add、与计划一致;④8bc3eb0 确在 main 祖先链(对测试方记录的更正成立);⑤compose 两侧 diff=0;⑥seed.ts deploy-demo 三点 diff 0 命中;⑦「占位边界」deploy-demo 0/main 1 命中(D2 手工补回必要);⑧合并树 amap/index.ts 三关键字 6 命中(N07 修复与 fetchWeather 并存);⑨main 版 command/page.tsx md5 与 6c21562 逐字节相同(取 deploy-demo 零损失);⑩eslint lib→{lib,shared} 属实(D4 下沉 shared 必要);⑪CI 现触发 deploy-demo 属实;⑫status-chip 在 screen/ 下 0 引用(N16 排除正确);⑬useScreenPoll(metric,initial,ms) 签名属实;⑭RESOLVERS 已含 weather:101(T2.3 零后端改动成立);⑮合并树 17 个 test 文件与 main 一致;⑯overview 无 ScreenAutoRefresh + fetchWeather revalidate 600 + AMAP_WEATHER_CITY:118 存在(D5/T3.3 属实)。**D1 破例批准**:默认 squash 的适用前提是 runner 工作分支的线性一次性改动,对已有 9 个被裁决记录引用 SHA 的长期分支不成立——squash/rebase 会使已提交决策依据变悬空引用,真 merge + tag + `-d` 安全网是正解;T5.1 的 `git branch --merged` 判据与 T6.1 显式禁令把破例钉死在本任务内,不外溢。**T5.4 高危项识别到位**:N13/N15 pass 不顺延、N16/N12 排除均经我独立复核。战术手册引用合规(docker-env-file 分析实质、双解析器耦合点登记待办;pg-job-queue/s3-direct-upload 不适用理由成立)。Gate(等 N12/N18/N19 复验完再开工)判断正确。无阻塞项,送人工审批 |
| r1 | 2026-07-29 | 主控 | 起草 | 送评审 | — | 基线 `main`=`aeb549b` / `deploy-demo`=`9befd9c`；勘察经 `git merge-tree --write-tree` 复核，未动工作树。修正测试方 N06 记录中 3 处不准确：① `main` 已有 `/screen/command`（`8bc3eb0`）且导航已 8 块；② compose 两侧 diff 为 0，不存在"带回 IPv4-only"；③ b-104 计划 deploy-demo 版**非逐字超集**，缺 `main` 侧 3 条评审结论。另修正分析中 2 处：N16/N12 不受换版影响（`status-chip` 在 `/screen` 零引用；N12 属后台路由且仍 `fix`）。 |
