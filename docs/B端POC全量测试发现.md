# B 端后台 · POC 全量测试发现（CDP 自动巡检）

> 2026-06-03，CDP 驱动浏览器逐页实测 25 页（截图 `/tmp/poc-*.jpg`，明细 `/tmp/poc-audit.json`）。**只记录，未改任何代码。**
> 分「功能」与「UI/规范」两类。引用已立项 bug 见 `docs/B端联调BUG清单.md`（B1–B17）。本文新增项标 `P-xx`。
> 全 25 页**无 JS 控制台报错**、**无裸 ISO 日期**（之前已修）。

---

## 一、系统性问题（跨多页，优先级最高）

### 功能
- **F1 · StatusChip 语义错配【严重·系统性】**：把各领域状态硬塞进"预约状态"的 启用/已暂停/已取消/已拉黑：
  - **设备列表**：OFFLINE→**"已取消"**、ALERT→**"已拉黑"**、ONLINE→"启用"（同页 KPI 却用正确词 在线/离线/告警，自相矛盾）。
  - **实时路况**：拥堵等级→"启用/已暂停"（路况等级 ≠ 预约状态，应畅通/缓行/拥堵）。
  - **内容列表**（B10）：草稿/已归档→"已暂停"、已发布→"启用"。
  - 根因：`status-chip.tsx` 只有预约/风控用的 8 个状态键，被全站复用。**建议**：按领域加状态键（设备 在线/离线/告警；路况 畅通/缓行/拥堵；内容 草稿/已发布/已下线）。
- **F2 · "已接入 SSE 实时更新"虚假【系统性】**：停车场、设备列表、设备详情都显示"已接入 parking_state/iot_event SSE 频道，实时更新"+绿点，但**页面内根本没有 `EventSource` 订阅**（全仓只有仪表盘 OccupancyCard 和 /realtime 真订阅）。→ 这些页"实时"是假的，数据不会自动变。（关联 B5/B11）
- **F3 · 登录表单提交失效**（B2/B15/B17）：点登录不跳转、回车不提交、密码错无提示。真人走登录页登不进（靠注入 cookie）。
- **F4 · 富文本编辑器不挂载**（B1）：所有内容新建/编辑页正文区永远占位框。
- **F5 · 仪表盘在园数 SSE 不更新 + 承载量口径错**（B4/B11）。

### UI / 规范
- **U1 · 红线 6 英文泄漏【系统性】**（B7 扩大）：
  - SSE 频道名：`checkin_event`（仪表盘）、`parking_state`（停车场×2）、`iot_event`（设备列表/详情）、`slot_changed`（大屏）；
  - **渠道标识枚举**：`MINI_PROGRAM / ONSITE_MAKEUP / OTA / ADMIN_MANUAL`（渠道接入页整列）；
  - `OTA`（预约单/渠道）；设备详情**完整 UUID** 暴露；预约编号露 `BK-SEED-` 种子前缀；原生日期控件英文格式 `06/03/2026`。
  - `scripts/lint-cn.mjs` 全没拦到——建议补"JSX 文案禁英文单词/枚举码"规则。
- **U2 · 高德地图未接入**：实时路况、停车场都是灰色占位；停车场文案"地图加载中…"是**永久假加载**（根本没在加载，误导）。

### 导航
- **N1 · 多个页面不在侧边栏菜单**：`预约单查询(/booking/bookings)`、`系统管理(/system)` 都**没有菜单入口**（面包屑只到"首页"），用户无法从导航到达。
- **N2 · 菜单坏链**：菜单"设备详情与心跳监测"指向**字面量 `/iot/detail`**，但真实路由是 `/iot/[deviceId]` → 会去加载 id=`detail` 的设备 → 不存在/报"设备不存在"。
- **N3 · 返回首页入口缺失**（B13）：logo 不可点；仅靠面包屑"首页"。

---

## 二、逐页发现

| 页面 | 功能 | UI/规范 |
|---|---|---|
| **01 仪表盘** `/` | 在园数 SSE 不更新(B11)、承载量口径错(B4)、"实时"绿点写死(B5)、告警设备灰点同离线(B6) | `checkin_event` 英文(B7) |
| **02 分时预约配额** `/booking/slots` | 渲染正常、真实数据 ✓ | — |
| **03 渠道预约接入** `/booking/channels` | 纯静态展示，无任何操作，状态恒"已启用"无意义 | 渠道标识整列英文枚举(U1) |
| **04 现场补录** `/booking/onsite` | 🔴 时段下拉"当日暂无可预约时段"，但 DB 当日有 5 ACTIVE 时段 → getOnsiteSlots 返空(疑日期/时区 bug) | 原生日期控件英文格式 06/03/2026 |
| **05 预约单查询** `/booking/bookings` | 列表/搜索/Tab 正常、状态 chip 正确 ✓ | **不在菜单(N1)**；OTA 英文、编号露 BK-SEED |
| **06/07 黑名单/申诉** `/riskcontrol/blacklist` | 渲染正常 ✓（B11 自动信号无异常） | — |
| **08–11 内容列表** news/intro/activities/knowledge | 列表真实 ✓ | 状态错配 草稿/归档→"已暂停"(F1/B10) |
| **12 富文本新建** `/content/news/new` | 🔴 编辑器不挂载(B1) | — |
| **13 活动编辑** `…/edit` | 同 B1 编辑器问题 | — |
| **14 报名审核** `…/signups` | 渲染正常 ✓ | — |
| **15 获奖公示** `…/awards` | 渲染正常 ✓ | — |
| **16–18 数据分析** traffic/heatmap/source | 真实数据 + 条形图 ✓ | — |
| **19 用户画像** `/analytics/profile` | 数据真实 ✓ | **描述栏露内部需求编号「B17总览/B18出行偏好/B19APP偏好」**(不该给用户看)；总览维度排序混乱(性别与年龄混排、年龄非递增、夹"18岁以下 0"空条)，应分组+有序 |
| **20 实时路况** `/traffic/road` | 高德地图未接入(占位)；路况数据疑写死(无路况表) | 拥堵等级→"启用/已暂停"错配(F1) |
| **21 停车场** `/traffic/parking` | "已接入 SSE 实时"虚假(F2)；地图未接入 | 假加载文案；`parking_state` 英文×2 |
| **22 实时设备列表** `/iot/devices` | "已接入 SSE 实时"虚假(F2)；延迟列全"—"(未取最新心跳) | 🔴 状态错配 离线→"已取消"/告警→"已拉黑"(F1)；`iot_event` 英文 |
| **23 设备详情** `/iot/[deviceId]` | "iot_event SSE 实时追加"虚假(F2) | 状态"启用"=在线错配；完整 UUID 暴露；面包屑只到"首页" |
| **24 系统管理** `/system` | 三 Tab 正常 ✓ | **不在菜单(N1)** |
| **25 数据大屏** `/realtime` | 🔴🔴 **实为开发调试页**：只有标题+"在数据库手动 UPDATE booking_slot 测试"调试说明+"等待事件…"，**无任何大屏可视化**；"○ 未连接"(SSE 没连)。PRD 的全屏数据大屏基本没做 | 满屏英文术语 slot_changed/Postgres NOTIFY/UPDATE/booking_slot |

---

## 三、新增待立项（P 系列，建议进 Bug 清单）
- **P1** 🔴 `/realtime` 数据大屏是调试页，无可视化（25 页最严重的功能缺失）。
- **P2** 🔴 设备状态错配：离线显示"已取消"、告警显示"已拉黑"（F1 最离谱实例）。
- **P3** 🟠 现场补录时段下拉为空但当日有时段（getOnsiteSlots bug）。
- **P4** 🟠 预约单查询、系统管理无菜单入口；设备详情菜单坏链 `/iot/detail`（N1/N2）。
- **P5** 🟠 路况"拥堵等级"显示成"启用/已暂停"。
- **P6** 🟡 停车场/设备"已接入 SSE 实时"虚假（页面无订阅）。
- **P7** 🟡 渠道接入页纯静态 + 英文枚举标识整列。
- **P8** 🟡 红线 6 英文泄漏面扩大（枚举码/UUID/OTA/原生日期格式），lint 未覆盖。
- **P9** 🟡 用户画像描述栏露内部需求编号 B17/B18/B19；画像维度排序混乱。
- **P10** 🟠 **时区处理不统一（系统性，代码审查发现，非页面巡检）**：
  - **根因**：进程未设 `TZ`（docker/Dockerfile/.env 均无 → 容器多半跑 UTC），且多处用 `new Date().toISOString().slice(0,10)` 取"今天"——`toISOString()` 恒返 UTC，**北京 0:00–8:00 这段会算成昨天**（设 `TZ` 也修不掉，因 `toISOString` 与进程 TZ 无关）。
  - **真 bug 实例**：① 现场补录默认日期 `booking/onsite/page.tsx:24`（**疑即 P3 时段下拉为空的诱因之一**——默认日期偏到昨天/明天，getOnsiteSlots 查不到当日时段）；② 导出时间范围 + 文件名日期 `api/export/[module]/route.ts:59-60,98`。
  - **待确认**：`analytics/repository.ts:35-36` 查询范围（若来自 `@db.Date` 则安全，来自墙上 `new Date()` 推算则偏）。
  - **本就正确、勿动**：`booking/domain/rules.ts:54`(canCancel)、`checkin/domain/rules.ts:43` 的 `slot.date.toISOString().slice(0,10)` —— `@db.Date` 经 Prisma 存为 UTC 零点，`.slice` 原样还原日历日，正确；其内联 `+08:00` 锚定北京墙上时间也是对的。`paidAt/blacklistedAt/createdAt` 等 `new Date()` 写时间戳全部正确（存的是瞬时，与时区无关）。
  - **建议（钉死中国时区，单区域标准解法）**：存储用 UTC（`@db.Timestamptz` 已是）/ 营业日用 `@db.Date`；业务计算 + 展示一律按 **Asia/Shanghai（=固定 +08:00，中国无夏令时）**，哪怕国外预订也按北京时间。落地：① docker/Dockerfile/.env 设 `TZ=Asia/Shanghai`（纵深防御）；② 建 `src/shared/lib/time.ts` 收口（`chinaToday()` / `cstStartOf()` / `formatCnDate()`「2026 年 6 月 5 日」/ `formatCnDateTime()`），禁散落的 `+08:00` 字面量与 `toISOString().slice` 取业务日；③ 修上述 2 处真 bug 改用 `chinaToday()`；④ `lint-cn.mjs` 加规则禁 `toISOString().slice` 取业务日 + 裸 `+08:00`（time.ts 豁免），防回归。
