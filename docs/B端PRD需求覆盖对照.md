# B 端后台 · PRD 需求覆盖对照矩阵（产品经理视角）

> 2026-06-04 起，以 **高层产品经理** 视角对照 `docs/PRD.md`（B 端在「一、平台管理后台」1.1–1.5）+「四、红线」逐条核**需求覆盖 / 业务逻辑 / 红线合规**。
> 与 `docs/B端联调BUG清单.md`（B 系列=联调缺陷）互补：本表记 **R 系列=需求/产品级问题**。
> 状态：✅ 已实现 · ⚠️ 部分/存疑 · ❌ 缺失 · 🚩 踩红线 · ⏳ 未验证
> 证据级别：〔码〕=已查代码 ·〔测〕=CDP 实测 ·〔待〕=待核

---

## 一、平台管理后台

### 1.1 基础宣传管理
| 需求点 | 状态 | 依据/问题 | 关联 |
|---|---|---|---|
| 景区介绍维护（图文/视频，多端发布） | ✅〔测〕 | 列表+CRUD+真实数据；视频素材/多端发布维度待核 | B10/B19/B20 |
| 活动运营管理（发布/报名审核/获奖公示/素材归档；报名费走微信且与入园隔离） | ✅⚠️〔测〕 | 列表+KPI+报名审核+获奖公示齐；蓝条明示"报名费由 C 端支付,B 端不触发支付"→**支付隔离红线达标✅**;**但 PRD 列的「素材归档」未见入口**(R-素材 待补);草稿态误显"已暂停"(B10) | B1(编辑器)/B10 |
| AI 问答知识库（语料维护+微调样本+闭环回流+人工纠偏） | ⚠️〔测/码〕 | 实测:`content/knowledge` = 语料 CRUD + 分类 + 关键词搜索(均正常,搜"园区"→1 条✓)。**但只有"语料维护"这一半**:PRD 要的**微调样本导出、闭环回流(用户提问回流入库)、人工纠偏(纠错标注)** 三项**全缺** → 实为普通问答 CMS，"AI"能力未落地 | R-AI |
| 资讯模块 | 🟡范围〔测〕 | **PRD 一期仅「PRD+UI，待确认再开发」**，但**实测已全量实现**:`content/news` 列表+CRUD+3 条真实数据+正式侧边栏菜单 → **超一期范围 / 需求确认**（R4）。内容红线干净(免费不售票/预约入园/身份证+车牌) | R4/B10 |

### 1.2 预约管理中心
| 需求点 | 状态 | 依据/问题 | 关联 |
|---|---|---|---|
| 分时预约配额配置（按日期类型规则/每日总库存/分时段上下线/单证单日上限） | ⚠️❌🚩〔测〕 | **实测:页名叫"配置"但实为纯只读监控表**——5 时段+分渠道名额(小程序/现场/OTA/后台)+在园 都能看,**但无任何 新建/编辑/配置 入口**。PRD 要的 ① 按日期类型(常规/节假日/特殊日)建规则 ❌、② 每日总库存配置 ❌、③ 分时段名额可编辑(上下线) ❌、④ 单证/手机号单日上限配置 ❌ **全缺**。仅承载达 90% 时有"恢复预约"按钮(ADMIN)。**名实不符=核心功能缺失** | R-cfg |
| 预约要素强校验（身份证+车牌，红线） | ⚠️〔码〕 | **③ 三路共用=确认成立✅**：`bookingService.createBooking` 是**唯一下单入口**(带 `channel` 参数,四渠道全走它)，统一过 `createBookingSchema`+`assertDualElements` → 任何渠道缺双要素即拒。**但校验强度不足仍在**：① 身份证仅 `length(18)`、**非格式/有效性校验**；② 车牌**无规则校验(含新能源)**。`countDailyBookings` 还实现了"同证当日已约即拒"(公平性✓) | R3 |
| 渠道预约接入（自营+高德/携程/美团库存切分；外部下单必带双要素否则拒收） | ⚠️〔测/码〕 | 页面**纯静态硬编码 4 行**(MINI_PROGRAM/ONSITE_MAKEUP/OTA/ADMIN_MANUAL)、状态恒"已启用"、**零管理操作**(P7)；渠道标识列直接露**英文枚举码**(U1/红线6)；库存切分委托给只读的 slots 页。**但**:外部下单双要素校验在 service 层**已强制**(见 R3 单入口)；真实 OTA 对接 API 未见 | P7/U1/R3 |
| 现场特殊人群补录（录身份证/护照+车牌，一键补录激活入园） | 🔴〔测/码〕 | 4 步向导(选时段/游客/车辆/确认)，下单走 ONSITE_MAKEUP 渠道+黑名单前置+双要素(R3✓)。**但 step1 时段下拉恒空**=当天无 slot(seed 只到昨天,**无每日时段生成机制**)→ **整链路当天瘫痪**(B23,真实预约同样受影响)。护照号入口未见(只 idCard)；日期控件英文格式(U1) | B23/R-cfg |
| 爽约风控安全锁（X 次/Y 天可配+自动拉黑+申诉人工复核） | ✅⚠️〔测/码〕 | **实现最完整的一页**：黑名单(身份证**脱敏✓**/车牌/原因/时间)+移除、申诉 tab+**人工复核(通过/驳回)✓**、自动拉黑("累计爽约3次自动加入")+人工加入 均真数据。**但风控规则比 PRD 简化**：① 阈值 `NOSHOW_BLACKLIST_THRESHOLD=3` **硬编码不可配**；② **无"指定天数内"时间窗口**(只看累计总数,非滑动窗口)；③ risk_blacklist **无 expiresAt → 永久黑名单**，缺 PRD 的"未来 Y 天后自动解封" | R-风控 |

### 1.3 出行服务（高德地图）
| 需求点 | 状态 | 依据/问题 | 关联 |
|---|---|---|---|
| 道路/矩形/多边形/周边 实时路况查询（4 类 API） | ❌〔测/码〕 | **确认全是桩**：`lib/amap.fetchTrafficConditions` 直接 `return [3 条写死路况]`(TODO 注释"替换为真实高德 REST API 需 AMAP_KEY")；地图=占位 div("待阶段5接入")；4 类 API 一个没有。**最后更新显示 now() 但数据是死的(误导)**。拥堵等级 StatusChip 错配:畅通→"启用"/缓行→"已暂停"/拥堵→**"已拉黑"**(P5/F1) | U2/P5 |
| 停车场动静态上图（满闲/余量） | ⚠️❌〔测/码〕 | 4 个停车场**真实 DB 数据**(总位/占用/余量/状态)✓。**但**:① **F2 假实时**——页面挂"已接入 parking_state SSE 频道,实时更新"+绿点,**实为纯 RSC、无任何 EventSource**(刷新才变)；② **地图永久假"加载中…"**(role=img,根本没在加载,U2)；③ 满车 FULL→StatusChip **"已拉黑"**、关闭→"已取消"(P2/F1 错配)；④ **parking_state 英文×2**(U1);⑤ 余量色用内联 style(B9)。高德地图未接入 | U2/F2/P2 |
| 拥堵分流（行前/行中停车场级引导） | ❌ | 未见任何实现 | R-分流 |

### 1.4 数据可视化与分析（B 端报表）
| 需求点 | 状态 | 依据/问题 | 关联 |
|---|---|---|---|
| 客流分析（多维对比/占比 + Excel） | ✅〔测/码〕 | **目前质量最高页**:真实 SQL 聚合+条形趋势图(中文日期/人次)+日期区间筛选+客流明细表。**Excel 导出已验真 xlsx**(MIME=spreadsheetml、魔数 PK、file 认作 Excel2007+、6.9KB)。小瑕:日期筛选用原生 date 控件英文格式(U1) | U1(轻) |
| 热力图分析（到访/客源热力） | ⚠️〔测/码〕 | **名不副实**:页名"热力图"、描述"游客密度热力分布",但**可视化实为条形图**(`BarList`,与客流分析同组件)，**无任何热力图(色块/网格/色阶)**。且维度偏:只做"各时段峰值密度"(hourly),**非 PRD 的「到访热力分布 + 客源热力分布」(地理热力)**。数据真实(getHourlyPeak)+导出在 | — |
| 来源分析（行政图，下钻省/市/区县） | ⚠️❌〔测/码〕 | **维度完全不对**:页名"来源分析"实为「**渠道分布**」(微信小程序/OTA/现场/后台,表头 渠道/数量/占比)，**无省/市/区县、无行政图、无下钻**。PRD 要的"地理来源行政图下钻"**没做**。**省市区可由身份证前6位(GB/T2260行政区划码)推**——代码已用身份证推性别(位17)/年龄(位7-10),推地区现成未做。数据真实+导出在 | R-来源 |
| 用户画像（基本特征/出行/APP 偏好） | ✅⚠️〔测〕 | 真实数据(身份证推性别/年龄)+三 Tab+导出。**P9 确认多毛病**:① 描述栏露**B17/B18/B19**内部编号;② 总览把**性别+年龄混在一个条形图**(应拆两图);③ **年龄段顺序乱**(18岁以下夹在18-30之后);④ 夹"18岁以下0"**空条**;⑤ **两组各自100%塞一图**→按最大值(性别920)归一,年龄条被压短**视觉误导** | P9 |
| **报表 Excel 导出（红线）** | ✅〔测/码〕 | `lib/excel` + `/api/export/[module]` + 四分析页均有导出按钮；**`/api/export/traffic` 实测产物=真 xlsx**(MIME 正确、魔数 PK、Excel2007+) → **红线实证达标**。其余 export/[module] 同机制,字段全度可抽验 | — |

### 1.5 物联网设备监控
| 需求点 | 状态 | 依据/问题 | 关联 |
|---|---|---|---|
| 实时设备列表（闸机/摄像头/广播/路灯全收录） | ✅⚠️〔测/码〕 | 5 台真实设备。**P2 状态错配最离谱**:StatusChip `ONLINE→启用/OFFLINE→已取消/ALERT→已拉黑`,而 KPI 同页用 在线/离线/告警 → **自相矛盾**;F2 假实时("已接入 iot_event SSE"+绿点,纯 RSC 无 EventSource);iot_event 英文×2(U1);离线行内联 style(B9);**无设备管理入口**(空状态"请先在数据库录入",R-只读第4例) | P2/F2/U1 |
| 状态与心跳（在线率/网络延时 ms/丢包率） | ⚠️❌〔测/码〕 | 在线/离线/告警计数有；但**延迟(ms)列恒"—"**(代码写死 `—`,从不计算)、**丢包率列根本不存在**。PRD 要的 网络延时/丢包率 均未实现 | — |
| 闸机核销联动（查预约库·当前时段有效·停车场车牌预扣车位） | ⚠️❌〔待〕 | `checkin` 模块存在；但 **B 端是否有核销联动/车牌预扣的业务可视化**待核（R6） | R6 |

---

## 四、关键红线复述 — 逐条合规
| 红线 | 状态 | 依据/问题 |
|---|---|---|
| 入园即预约、全流程无"门票/票价/退款"字样 | ⏳ | 暂未撞见票务词(资讯/活动内容实测均用 免费不售票/预约/名额/核销 等正确措辞)，**仍待全站系统核** |
| 预约双要素强约束（身份证+车牌，三路共用，后台双校） | ⚠️ | 见 1.2 R3：**三路共用=确认成立✅**(单一 createBooking 入口强制双要素)；唯**校验强度不足**(身份证仅长度、车牌无规则) |
| 支付链路隔离（入园主流程不调支付） | ✅ | 活动页明示报名费走 C 端、B 端不触发支付 |
| 预约公平性（单证单日上限 + 爽约风控） | ⚠️ | 爽约风控页**实现完整**(自动拉黑+申诉复核✓)，但阈值硬编码(3)、无天数窗口、永久黑名单(无Y天解封)；单证当日已约即拒✓(R3),但单日"上限N"可配性无 |
| **承载力 90% 熔断（瞬时承载量）** | 🚩 | **B4：分母用"当日各时段容量累加(1050)"，致 90% 熔断几乎永不触发，红线未真正落地** |
| 报表 Excel 导出 | ✅ | 见 1.4 |
| 100% 中文（无孤立英文/ISO 日期） | 🚩 | **U1：英文泄漏面广（SSE 频道名/渠道枚举码/UUID/OTA/原生日期格式），lint 未拦** |

---

## 产品级待办（R 系列汇总）
- **R3** ⚠️ 预约双要素：**三路共用已确认**（单一 createBooking 入口强制）；仅剩**校验强度不足**（身份证只 length18、车牌无规则/新能源）待加强。
- **R-渠道** 🟡 渠道预约接入页纯静态硬编码、无管理能力、露英文枚举码(P7/U1)；真实 OTA 对接 API 未见。
- **R-只读 🚩〔系统性·用户发现〕「管理/配置」类页面普遍只读、无编辑入口**：已确认 ① 分时预约配额配置(slots,R-cfg)、② 渠道预约接入(channels,R-渠道)、③ 停车场动静态上图(parking,自承"请先在数据库录入停车场信息")、④ **实时设备列表(iot/devices,自承"请先在数据库录入设备信息")** 四页都**只显示、不能 新建/编辑/配置**。需逐页排查所有"管理页"是否只读空壳并补齐 CRUD/编辑入口。停车场尤需管理页(名称/容量/坐标/状态);设备同理(名称/类型/位置/编号)。
- **R4** 🟡 资讯模块超一期范围（PRD 仅 PRD+UI 待确认，却全量实现+挂菜单）—— 需范围确认。
- **R-素材** ⚠️ 活动运营管理缺「素材归档」入口（PRD 1.1 明列），其余三项(发布/报名审核/获奖公示)齐。
- **R6** ⚠️ 闸机核销联动 / 车牌预扣车位 的 B 端业务可视化缺失待核。
- **R-AI** ⚠️ AI 知识库**只有语料维护**(CRUD/分类/搜索 正常)，**微调样本导出/闭环回流/人工纠偏 三项全缺**——"AI"能力未落地，目前等于普通问答 CMS。
- **R-cfg** 🚩 分时预约配额"配置"页实为**纯只读**——建规则/总库存/分时段名额编辑/单证单日上限**全不可配**，名实不符，核心配置功能缺失。
  - **〔用户决策·UX 2026-06-04〕日历视图优先 + 切换表格视图**：重建为真正可配的工具时，**默认日历视图**(月/周，每日显示名额/占用，点某天看/改时段，按区间套常规·节假日·特殊日规则)，提供切换键回到现表格视图(单日明细)。日历是配"按日期类型建规则"的天然界面。
- **R-承载口径** ⚠️ 承载熔断算法两处不一致:slots 页按**单时段** checkedIn/capacity≥90%、仪表盘按**当日容量累加**(B4)；PRD 要"瞬时园区承载量",两者都不等于它，需统一定义。
- **R-分流** ❌ 拥堵分流 未实现（PRD 1.3 第 6 项：综合停车/交通大数据，行前行中做停车场级分流引导）。
- **R-地图UX 〔用户决策 2026-06-04〕** 出行服务（路况/停车）页应**以高德地图为整页背景**，路况/停车数据做**地图上的浮层小组件**（图例/KPI/路段拥堵标注/停车场标点/分流建议卡），而非现在的"小占位图+KPI卡+下方表格"。地图是主体、数据叠加其上。适用 `/traffic/road` 与 `/traffic/parking`。前置:R-config 高德 Key + 地图 JS API 接入。
- **R-config** ⚠️ 缺**第三方集成配置中心**（微信小程序/公众号/支付、AI、高德 key 等密钥统一配置位）；system 模块仅 RBAC、无设置表。多功能共同前置。
- **R-mp** ⚠️ 内容→小程序兼容（mp-html 白名单 + 图片公网可达）须在产真内容前立护栏；现 seed 纯文本占位、暂无问题。
- **R-来源** ⚠️ "来源分析"实为渠道分布，**PRD 的省/市/区县行政图下钻未做**。省市区数据源=**身份证前6位行政区划码**(代码已用身份证推性别/年龄,推地区现成);行政图可配高德/echarts map。同时还缺**热力图本体**(到访/客源地理热力,见 1.4 热力图行)。
- **R-画像UX 〔用户决策 2026-06-04〕** 用户画像偏好类用**雷达图**(多维偏好天然适合);性别用饼/环图、年龄用条形(正确排序、去空条),**三类拆开别混一图**(修 P9)。
- **R-设备UX 〔用户决策 2026-06-04〕** 实时设备列表改**卡片网格平铺**(每设备一张小卡:名称/类型/位置 + 状态色点 + 心跳时间/延迟ms),替掉表格,监控类卡片更直观好看;状态色一并修 P2(在线绿/告警黄/离线灰)。
- **R-图表选型〔系统性〕** analytics 全用 `BarList` 一种图,与数据不匹配:客流趋势→bar/line✓、热力图→heatmap、来源→行政 map、画像偏好→radar、占比→pie。应按数据配图,别一律条形。
- **R-补录** 🔴 现场补录界面定位错：当前 4 步向导属**手机端**形态；B 端应是 ① **补录记录监控列表**(谁/操作员/何时) + ② **电话兜底补录**(管理员代录，**AI 一句话填表**，复用 R-AI/R-config)。双要素/黑名单仍强校验。
- **R-风控** ⚠️ 爽约风控实现完整(自动拉黑+申诉复核✓)，但规则简化:阈值=3 硬编码不可配、**无"指定天数内"时间窗口**(只累计)、**永久黑名单无 Y 天自动解封**。阈值/天数应进配置中心(R-config)。
- **R-申诉闭环** ⚠️🔴 **申诉"提交"端断裂**：后台能复核，但**前端无申诉提交渠道**(仓库无 C 端/小程序工程)，且后端 `submitAppeal` **service 有、却未暴露成 API**→ 游客无处递交申诉；现申诉 tab 数据为 seed。需 ① 暴露 submitAppeal API ② C 端做申诉提交页(用户决策:同团队做)。
- **承载力熔断(B4)/中文红线(U1)** 两条已踩红线，见上。

> 说明：UI/样式类问题统一汇入 `docs/B端联调BUG清单.md` 顶部「设计系统统一策略」，最后一次性调 theme，不在本表展开。
> **范围说明〔用户决策 2026-06-04〕**：仓库当前**只有 B 端**，**C 端游客小程序未建**。凡"游客端提交/触发→后台处理"的闭环(申诉提交、AI 问答、在线预约、内容多端展示…)，B 端这半能测、C 端那半普遍缺。**下一轮补上小程序、与 B 端一起测闭环**；本轮先把 B 端逐页走完。"C 端缺"不计为 B 端 bug。

---

## R-editor · 内容编辑器选型 + 小程序预览〔用户决策 2026-06-04〕

**背景**：用户希望内容编辑达到"最新版 WordPress 那种好用"，但硬约束是**生成内容必须能在微信小程序正常显示 + 高度可定制**，且**编辑时要能预览小程序里的效果**。

**关键事实**：微信小程序**渲染不了任意 HTML**（无 DOM、不能 `dangerouslySetInnerHTML`）。WordPress/古腾堡产出的带 class/内联样式/自定义区块标记的 HTML 到小程序直接废。可选：原生 `rich-text`（白名单子集，定制极低）／`mp-html` 解析库（覆盖大部分图文，定制中）／结构化 JSON + 自写渲染器（定制极高、成本最高）。

**已定方向（据用户答复：内容文章型为主 + 小程序同团队做）**：
- 采用 **A 档·经典版**：B 端**保留 TipTap**（已在栈），输出**受限 HTML 白名单子集**（标题/段落/粗斜/列表/引用/图片/视频/链接，无任意 class/自定义区块标记）。
- **HTML 当内容真源**（现模型即如此）→ **零数据迁移**；不上区块 JSON（文章型不需要，且会逼两端重做渲染 + 迁移）。
- **C 端小程序用 `mp-html` 渲染同一套 HTML**；同团队做 → 编辑器白名单对齐 mp-html 支持范围，渲染保真，定制走"标签→样式"小映射表（内部约定，非跨团队契约）。

**小程序预览（用户新增要求）**：
- B 端编辑器加**手机框预览面板**：用与 mp-html **同一套标签样式映射**把当前 HTML 渲染进模拟小程序外壳 → 所见即小程序所得。保真度靠"预览样式 == mp-html 样式"这一份共享映射维持。难度低-中（约 1–2 天）。
- 真机预览（WeChat 开发者工具扫码）属 C 端工作流，需小程序工程 + 微信 CLI，**本期 B 端先做模拟预览**，真机预览待 C 端推进。

**难度/工作量（具体）**：
| 活儿 | 难度 | 备注 |
|---|---|---|
| 媒体上传+存储（真长杆，现为零） | 中 ~2–3 天 | 先定存储:联调期本地盘挂卷最省、上线换 OSS;写上传路由 + 编辑器接传图/传视频 |
| TipTap 补全到经典版 + 输出白名单 | 中 ~2–4 天 | 加上传入口/对齐/封面图选择器/发布侧栏 |
| 修 B1 编辑器挂载 | 低 ~0.5–1 天 | 先决条件,prod 容器待复测 |
| 小程序模拟预览面板 | 低-中 ~1–2 天 | 共享标签样式映射 |
| C 端 mp-html 渲染 | 中 ~2–3 天 | 做小程序时一并 |

> B 端侧合计约 1 周;比古腾堡(多周+持续维护)省一个量级,且满足"像最新 WP + 小程序兼容 + 高度定制 + 可预览"四诉求。

### R-storage · 媒体对象存储〔用户决策 2026-06-04，已落地基建〕
**决策**：
- **MinIO 仅开发/联调用**（本地自托管、零成本起一个 S3 兼容服务），**不是生产存储**。
- **应用层一律走 S3 SDK（`@aws-sdk/client-s3`）**，不写任何 MinIO 专属代码 → 靠 S3 协议天然多平台兼容：**开发对 MinIO、生产对 腾讯云 COS / 阿里云 OSS（或 AWS S3）**，切换只改 endpoint + key + bucket(+ region/path-style)，**业务代码零改动**。
- 即"面向 S3 接口编程，MinIO/COS/OSS 都只是其后端实现"。

**已落地（docker compose）**：
- `minio` 服务：`server /data`，S3 API 绑 `0.0.0.0:9000`（媒体经 VPN `10.7.0.1:9000` 可访问）、控制台 `127.0.0.1:9001`；资源封顶 cpus0.5/512m；卷 `minio_data`。
- `minio-init` 一次性：建桶 **`changqiushan-media`** + 设公共读（图片 URL 可直接嵌内容）。已实测桶就绪、`/minio/health/live`=200。
- 默认凭据 `MINIO_ROOT_USER/PASSWORD`（compose 默认 `changqiushan` / `changqiushan_minio_dev`，prod 应在 `.env` 覆盖）。

**待 upload 代码读取的存储 env 契约（建议）**：
```
STORAGE_DRIVER=s3
STORAGE_ENDPOINT=http://minio:9000          # 容器内网名;prod 换 COS/OSS 域名
STORAGE_REGION=us-east-1                     # MinIO 任意;COS/OSS 填真实 region
STORAGE_BUCKET=changqiushan-media
STORAGE_ACCESS_KEY=...                       # MinIO root 或子账号;prod=云厂商 AK
STORAGE_SECRET_KEY=...
STORAGE_FORCE_PATH_STYLE=true               # MinIO 必须;COS/OSS 视情况(多用虚拟主机式)
STORAGE_PUBLIC_BASE=http://10.7.0.1:9000/changqiushan-media   # 拼可访问 URL;prod=CDN/桶域名
```
**实现提示**：用 `@aws-sdk/client-s3`(+ `lib-storage` 分片、`s3-request-presigner` 签名)；一套代码通吃 MinIO/COS/OSS。COS S3 端点 `cos.<region>.myqcloud.com`、OSS S3 端点 `oss-<region>.aliyuncs.com`（OSS 的 S3 兼容个别能力有差，必要时退回原生 SDK）。
**上传流程 = 预签名直传（presigned URL direct upload）〔用户决策 2026-06-04〕**

目标：**文件字节不经过应用服务器，浏览器直传对象存储，省服务器流量/带宽费用**。

```
① 浏览器 → 服务器：请求上传(带 文件名/类型/大小)
② 服务器(持密钥,服务端签名) → 浏览器:返回 presigned PUT URL(限时+限类型+限大小)
③ 浏览器 → S3/MinIO/COS/OSS：HTTP PUT 直传文件(不过应用服务器,省流量)
④ 浏览器 → 服务器：提交(带 object key)
⑤ 服务器 commit：HEAD 校验对象存在+大小/类型 → 落库记录 → 返回可访问 URL
```

**要点**：
- **签名只在服务端**（用 `@aws-sdk/s3-request-presigner` 的 `getSignedUrl` + PutObjectCommand），AK/SK 永不下发浏览器。
- presigned URL 加约束：`expiresIn`(短时效)、`ContentType`、必要时 `ContentLength` 限制，防滥用。
- **桶要配 CORS**：允许应用 origin 的 `PUT`（MinIO/COS/OSS 都需配；否则浏览器直传被拦）。
- **commit 步**：服务器 `HeadObject` 确认上传成功 + 校验大小/类型后才落库；可先传到 `tmp/` 前缀、commit 时再视为正式，配生命周期规则 GC 掉未 commit 的孤儿对象。
- 跨平台：presigned PUT 在 MinIO/COS/OSS/AWS S3 通用（S3 SDK 一套）。公共读桶可直接拼 URL；私有桶则 commit 后发 presigned GET。

**S3 目录(前缀)结构 + commit 移动 + 成本控制〔用户决策 2026-06-04〕**

桶内按"公开/私有 + 模块"分前缀治理，**只有 `public/` 公共读，其余一律私有**（访问走 presigned GET）：
```
changqiushan-media/
├── staging/                       # 所有上传统一先落这里(临时区), 不公开
│   └── <yyyymmdd>/<uuid>.<ext>
├── public/                        # ← 仅此前缀设匿名公共读
│   ├── avatar/<userId>.<ext>      #   用户头像(明确放 public)
│   └── content/<module>/<id>/…    #   需公开访问的内容配图(景区介绍/资讯等文章图)
└── private/                       # 默认私有, 访问发 presigned GET
    └── <module>/<…>               #   按模块隔离的私有文件
```

**上传统一落 staging，commit 时再移动到目标前缀**（呼应上面的 presigned 流程）：
- 直传一律 PUT 到 `staging/<date>/<uuid>`；浏览器提交后服务器 commit。
- commit：`HeadObject` 校验 → 按业务定目标前缀(头像→`public/avatar/`、文章图→`public/content/<module>/`、私有→`private/<module>/`) → **`CopyObject` 到目标 + `DeleteObject` 删 staging**（S3 无原子 move，用 copy+delete）→ 落库最终 key/URL。

**成本控制（长期存储费用是大头）**：
- **`staging/` 生命周期规则**：N 天(如 7 天)自动过期，GC 掉只传未 commit 的孤儿对象（MinIO `mc ilm` / COS/OSS 生命周期）。
- **删内容即删对象**：内容/头像被删除时同步删 S3 对象，避免"已无引用仍在计费"。
- **冷数据转存储类**：长期不访问的可转低频/归档存储类(IA/Archive)降单价（COS/OSS/S3 均支持）。
- **去重**(可选)：按内容 hash 命名，相同文件不重复存。

**权限策略落地**：匿名公共读**只授予 `public/` 前缀**（MinIO `mc anonymous set download …/public`；COS/OSS 用 bucket policy 限定该前缀）；`staging/` 与 `private/` 不公开。

**注**：当前仅基建就绪（MinIO + 桶 + public 前缀策略），应用的"签发 presigned URL 路由 + commit(移动)路由 + 生命周期规则 + 编辑器直传接入"仍是后续 dev 工作（见上表工作量）。

### R-AI · 外接 AI 模型接入 + 配置〔用户决策 2026-06-04〕

**背景**：AI 问答知识库要真正"AI"，需外接 LLM（PRD 1.1 闭环回流/人工纠偏/微调样本、2.4 小程序 AI 问答的后端）。需**一处配置模型 Key + 地址**。

**决策**：
- **用 OpenAI SDK（`openai` npm，未安装）**，关键是其支持自定义 `baseURL` → 一套代码接所有 **OpenAI 兼容**服务：OpenAI / Azure OpenAI / DeepSeek / Kimi / 通义千问兼容模式 / 本地 Ollama·vLLM。与 [R-storage] 的 S3-SDK 同思路——**面向兼容接口编程，换 provider 只改 baseURL+key+model**。
- **配置位置**：在 **系统管理(`/system`)** 新增「AI 模型配置」(`baseURL` / `模型名` / `API Key` / 超时·温度等)。**现状 system 模块只有 RBAC 表(SysProfile/Role/Permission…)、无设置表** → 需新建配置存储（`SysSetting` 键值表 或 专用 `AiModelConfig` 表）。
- **Key 安全（红线）**：API Key 是密钥——**仅服务端调用、绝不下发前端**；DB 存须**加密**（env 主密钥 AES 加密，UI 脱敏显示 `••••`），或 Key 走 env、非密配置(baseURL/model)走 DB。

**接入后才能补齐 R-AI 三缺口**：
- 微调样本导出：知识库问答对导出为训练样本(jsonl)。
- 闭环回流：小程序用户提问 → 调模型答 → 未命中/低置信的问题入库为**候选语料**。
- 人工纠偏：对模型回答**打标/纠错** → 修正后回流知识库（作 system prompt 知识 或 微调样本；不做 RAG）。

**产品形态〔用户决策 2026-06-04〕：标准 AI 对话框 + 历史 → 直接长成知识库（UI/UX 参考 Google Gemini）**
- AI 智能问答 = **标准对话界面**（不是手工录 Q&A 表），**参考 Google Gemini**：
  - 对话流（用户/AI 气泡）+ **流式输出**（token 逐字打出）；
  - **左侧历史会话**列表（按时间分组、可检索）= "历史问题"持久化；
  - 回答 **Markdown 渲染**（代码块/列表/表格）；多行输入框 + "新建对话"；
  - **回答操作：复制 / 重答 / 赞·踩 / 编辑** —— 赞踩+编辑即"人工纠偏"入口；
  - 〔校正 2026-06-04〕**C 端 AI 不做 RAG**（直连大模型，景区知识作 **system prompt** 背景注入；无向量检索/知识库召回/**来源引用**）。故 Gemini 式对话**去掉"来源引用"**。详见记忆 `project_cside_ai_no_rag` + 计划 `.claude/plan/c-05-ai-chat.md`。
- **历史 → 知识库（闭环）**：真实对话沉淀 → 人工从历史里**精选/纠正 → 提升为知识条目** → 反哺 **system prompt 知识 / 微调样本**（非 RAG 召回）。即知识库**从真实问答里长出来**，而非纯手工录入。
- 现有 `/content/knowledge` 手工 CRUD → 演进为"知识库（含从对话提升的条目）"，并新增 **AI 对话页（Gemini 式）** + 会话历史表。

**工作量/难度**：
| 活儿 | 难度 | 备注 |
|---|---|---|
| 装 `openai` + 服务端 chat 调用封装(可换 baseURL,流式) | 低 ~1 天 | 一套 client，多 provider |
| 系统管理「AI 模型配置」表 + UI(含 Key 加密/脱敏) | 中 ~2 天 | 新建 SysSetting/AiModelConfig |
| **AI 对话页(Gemini 式:对话流/流式/历史侧栏/Markdown/赞踩/引用)** | 中-高 ~3–5 天 | 前端大头 |
| 会话/消息持久化(session+message 表) | 中 ~1–2 天 | = 历史问题 |
| 景区知识装配为 system prompt(无 RAG/向量检索) | 低 ~0.5 天 | C 端直连大模型,知识作背景 |
| 历史→知识 提升/纠偏工作流 + 微调样本导出 | 中-高 ~按子功能 | 闭环落地 |

**范围**：PRD 一期含 AI 问答(1.1 + 2.4)，接入在范围内。**注**：当前 openai 未装、无配置表/对话页/会话表，均为后续 dev 工作。

### R-config · 第三方集成配置中心〔用户决策 2026-06-04〕

**背景**：多处第三方集成都需要"一处配置密钥/参数"，但 **system 模块当前只有 RBAC 表、无任何设置/配置表**。零散加会重复造轮子，应建**统一的系统集成配置中心**（系统管理下）。

**需收纳的配置（均为密钥，统一安全模式）**：
| 集成 | 关键参数 | 用途 / PRD |
|---|---|---|
| **微信小程序** | AppID / AppSecret | 游客端登录(code2session)、获取 access_token、消息下发；PRD 二章小程序 |
| **微信公众号** | AppID / AppSecret / Token / EncodingAESKey | 公众号消息回调/模板消息/菜单 |
| **微信支付** | mch_id / API v3 key / 商户证书 | 活动报名费(二消)；PRD 红线"支付仅二消" |
| **AI 模型** | baseURL / model / API Key | AI 问答(见 R-AI) |
| **高德地图** | Key (web服务/JS) | 实时路况4类API + 停车场上图;PRD 1.3 |
| 对象存储(可选纳入) | endpoint/key/bucket | 媒体(见 R-storage;也可仅走 env) |

**统一安全模式（红线）**：全部是密钥 → **仅服务端读取、绝不下发前端**；DB 存须**加密（env 主密钥 AES）+ UI 脱敏 `••••`**；或密钥走 env、非密参数(AppID/baseURL 等)走 DB。

**落地**：system 模块新增 `SysSetting`(分组键值:group/key/value/encrypted) 或按集成建专表；系统管理加「集成配置」页（分 微信小程序/公众号/支付/AI/高德 等子区）。R-AI 的"AI 模型配置"并入此中心。

**注**：当前全缺（无配置表、无配置页）。这是后续 dev 工作，且是**多个功能（小程序登录、AI、地图、支付、内容图片公网可达）的共同前置**。

### R-mp · 内容 → 微信小程序兼容（贯穿所有富文本内容）
**要求**：景区介绍/资讯/活动详情/知识库的富文本最终在小程序用 `mp-html` 渲染 → 每页内容须过"小程序兼容"这关。
- **检查法**：① 抓库里富文本 HTML 的标签集合，比对 mp-html 白名单（非白名单/script/iframe/自定义标签=风险）；② 抓 `<img src>`、`cover_image` 看是否**公网可达**（localhost/相对/内网 URL 小程序读不到）。
- **当前状态**：seed 内容全是**纯文本占位**、无图片 → 暂无越界标签/坏图，但**真内容尚未产生**。
- **调整（护栏，须在产内容前立好）**：编辑器**输出受限白名单 + 存盘消毒**（R-editor）；图片**传 public 桶取公网 URL**（R-storage）；**小程序模拟预览**校验（R-editor）。

### R-渠道 · 渠道接入与业绩统计〔用户决策 2026-06-04〕

**产品形态**：渠道预约接入页从"静态图例"重塑为 **渠道业绩统计看板**。
- **配置接入**：在**系统配置中心(R-config)** 配各渠道（注册渠道 + 密钥/参数，加密）。
- **实际对接**：携程/美团/百度地图等**逐个定制开发**（每个外部系统一套）——**本期不做**。
- **本页展示**（接入后）：各渠道**按日产生多少预约/订单** → 看**哪个渠道好**；要**历史** + **图表（柱状/曲线/饼图）**统计。
  - 柱状：渠道间某期对比；曲线：各渠道随时间趋势；饼图：渠道占比。
**DB 设计（用户校正 2026-06-04）：基础事实表 = 现有 `booking` 表，统计在其上聚合**
- **基础表已存在**：用户指出基础表 = "谁/何时/哪个渠道/预约哪天"的明细，**正是 `booking` 表**——
  - 谁 `visitorName/idCard/phone` · 何时 `createdAt` · 哪个渠道 `channel`(枚举) · 预约哪天 `slotDate`(@db.Date,已反范式) · 结果 `status/checkedInAt/cancelledAt/noShowAt`。
  - → **无需新建基础表**；之前提案的"日聚合表"理解有误（那是预聚合、非基础），**撤销**。
- **统计在基础表上聚合**：渠道业绩 = `SELECT channel, slotDate, count(*) … FROM booking GROUP BY channel, slotDate`（或按 `createdAt` 维度）→ 各渠道按日 订单数/核销/取消/爽约 → 图表(柱/曲线/饼)。"哪个渠道好"= 订单高+核销高+爽约低。
- **可选优化（非必须）**：① 量大时加**物化视图/日快照** `booking_channel_daily_stat`(channel+date→counts) 提速，pg-boss 每日刷新(对齐 CLAUDE.md)；② 外部渠道(携程/美团/百度)接入前，`channel` 枚举只有 4 值——若要区分外部来源，再加**渠道注册表** `booking_channel`(code/name/type/enabled)，并把 `booking.channel` 扩成引用。**这两项都待真接入时再做**。
- **结论**：基础数据齐了，现在缺的只是**统计查询/视图 + 图表看板 UI**，不缺基础表。实际接入逻辑不做。是否现在搭统计查询/看板，待用户拍板（属 dev）。

### R-补录 · 现场补录界面定位纠正〔用户决策 2026-06-04〕

**现状判定：当前 B 端 `/booking/onsite` 形态做错了。**
- 现在是 **4 步向导式现场录入**（选时段→游客→车辆→确认）——这是**手机端**形态（现场工作人员手持设备给未预约游客补录），**不该放在管理后台**。

**B 端管理后台的"补录"应是两个目的：**
1. **管理/监控**：展示**现场补录了哪些**——ONSITE_MAKEUP 渠道的补录记录列表（游客/身份证/车牌/时段/**操作员**/补录时间），供后台掌握现场补录情况。
2. **电话补录（兜底）**：现场补录不了时，游客**打电话给后台管理员**，管理员**代为补录**。
   - **AI 辅助录入**：管理员**一句话**（自然语言，如"张三 身份证110…1234 车牌川A12345 约明天上午场"）→ **LLM 抽取结构化**（姓名/身份证/手机/车牌/时段）→ **自动填好表单** → 人工核对 → 提交。
   - 复用 **R-AI / R-config** 的 LLM 能力（同一模型配置）。
   - **双要素(R3)+黑名单**仍在提交时强校验，AI 只负责"填"、不绕过校验。

**关联/前置**：B23（当天无时段→电话补录也要有时段可选，需先解决时段排期）；手机端现场补录形态归 C 端/小程序工作流。
