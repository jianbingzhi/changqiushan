# 长秋山 B 端 · 未实现/遗留事项 单一汇总清单

> **唯一入口**:所有"还没做完"的事项收口到这一份,避免分散在多文档导致遗漏。
> 详情仍在原文档(本清单每项标注来源);**新发现的 issue 先登记到这里**,再按需展开到原文档。
> 状态:🆕 未做 · 🛠️ 部分(有未完成部分) · 🧪 待实测(代码已落,待 QA docker 实测) · ⛔ 阻塞(外部依赖)
> 最后更新:**2026-06-10**(b-103 落地:T1 高德全栈+T2 富文本视频+T3 theme QA+T4/T5/T6/T7 全关单;外部余项=高德白名单·真机播放·Vercel cron 确认)。早期 b-100 见 `.claude/plan/b-100-...`、b-101 见 `b-101-...`、b-102 见 `b-102-待办执行计划.md`。
>
> **🔧 当前待处理任务（执行计划用·已剔除 b-102 完成项）**：
> | # | 任务 | 优先 | 状态 | 前置/阻塞 | 范围 |
> |---|---|---|---|---|---|
> | **T1** | **高德地图集成(R6)** | 高 | 🧪 **代码侧已完成(b-103 1a–1e)**:REST 三态/POI seed×8/loader+AmapContainer/road·parking 整页地图/大屏地图层+staticmap 代理 | ⛔ 高德控制台域名白名单(加 `changqiushan.vercel.app`+localhost)→ 线上 JS 层验收 | `infrastructure/amap`·`lib/amap`·`traffic/*`·`screen/*` |
> | **T2** | **富文本传视频**(B25残留) | 中 | 🧪 **代码+docker e2e 已完成(b-103 2a–2e)**:mp-html 换芯/sanitize 闸门/mp4 kind 分叉/VideoNode 编辑器/端到端 mp4 入桶→消毒→C端API→公网读全通 | ⛔ 真机小程序 detail 播放 + 历史排版回归(微信开发者工具) | `editor`·`storage`·`miniprogram` |
> | **T3** | **theme 深浅双主题全站视觉回扫**(B9/14/18/19/20/24) | 中 | 🧪 b-103 截图矩阵已跑(docker 生产构建),issue 见 §一 theme-QA 段 | — | 全`(admin)`页 |
> | **T4** | **大屏/screen 读路径同B34改派生** | 中 | ✅ b-103 完成(8 处迁 listSlotsForDate,grep 收尾合预期) | — | `screen/*`·`api/screen/[metric]` |
> | **T5** | **B26收尾:删废弃 cron 预生成** | 中 | ✅ b-103 完成(7 处删+copyDaySlots 逃生口+unschedule+horizon_days 两库迁移);⛔ 仅剩 redeploy 后 Vercel 面板确认 cron 列表 | — | `booking`·`quota-rules` |
> | **T6** | **内容文案红线1审改** | 低 | ✅ b-103 完成(seed 改写+docker/Supabase 存量各改 1 行+grep 取证 0 残留) | — | `prisma/seed.ts` |
> | **T7** | **B16 会话滑动续期** | 中 | ✅ b-103 docker QA 三场景全 PASS(S1 续期不掉线/S2 停 GoTrue fail-closed/S3 ban 后续期被拒;exp 已还原 604800) | — | 认证 |
> | **T8** | **承载熔断真实数值(B4)** | — | ⛔ 待PRD给瞬时承载量数值 | 业务 | SysConfig |
> | **T9** | **大屏C1–C7占位项**(地域画像/天气AQI/POI计数/漏斗浏览量等) | 低 | 🔧 各有外部前置 | 见§五 | `screen/*` |
>
> **✅ 本轮已完成(不需做)**:b-102(commit `ee0cdfa..bacb580`)实现 B26时段派生/B27日期控件/B31配额日历+规则层/B32分页/B33行政图+热力+只读CRUD + B34读路径;b-101 实现 B25/B30 文档编辑(图片上传/文档模式/气泡/消毒/拖拽,仅视频→T2);Vercel demo 已配:15时段模板、图片直传S3key、高德三件套env。详见下文各条 ✅。
>
> **📄 注**:§一 历史条目状态已于 2026-06-10(b-103 步骤0)对齐 b-102 实况(B26/27/31/32/33 → ✅);任务状态以上表为准。
> **2026-06-09 本轮要点**:① B25 内容编辑文档化(图片上传/文档画布/气泡/消毒/拖拽)**已实现并推送线上**(commit bcdb4d9),仅 视频 + Vercel 图片直传 S3 key 待办;② 逐页回归新增 B31/B32/B33;③ B26 时段生成升级为设计重构、B23 cron 方案作废。

---

## 一、功能 / 上线前(优先)

> **2026-06-08 二轮 CDP 真登录线上复核新增(B25–B28,详见 BUG清单)**:

- 🛠️ **B25/B30 内容编辑文档化改造** —— 🟠→**大部分已落并推送线上**(commit bcdb4d9,计划 `.claude/plan/b-101-...`)。**已实现 + CDP 线上/ docker 实测通过**:① 封面 URL→**上传**(预签名直传)+ 编辑器插图改上传(去 URL)+ 拖粘直传;② **文档画布范式**(封面 banner / 内联大标题 / 属性侧栏,取代分字段表单);③ **气泡工具栏**(选中浮现);④ **正文白名单消毒**(`sanitize-html`,每个 `save*` 入口强制,白名单对齐 Taro `<rich-text>`);⑤ intro/knowledge **列表拖拽排序**(删表单 sortOrder 框)。**〔2026-06-10 b-103 T2 补完〕视频已落**:mp-html 换芯(分包)+sanitize 闸门(仅自家桶 mp4)+存储 kind 分叉+编辑器 VideoNode/上传按钮/拖粘,docker 端到端通;**验收口径**:仅支持 H.264 编码 mp4(无转码基建,产品约定)、上限 env 化(Vercel 须设 `MAX_VIDEO_UPLOAD_BYTES=52428800` 对齐 Supabase 免费档 50MB)、**intro/news/knowledge 三类正文小程序无渲染页,视频暂仅 B 端可见**(C 端消费面=activities detail);⛔ 真机播放回归待用户。线上图片直传已通(见§四)。来源:BUG清单 B25/B30。
- ✅ **B26 当天零时段 → 时段生成策略重构(去 cron 单点)** —— **〔2026-06-10 对齐〕b-102 已完成根治**:规则派生(基准+特例、查询时即时解析、闭园=0)+ `booking_slot` 首单惰性物化 + B34 补完管理视图/C端读路径,线上/docker 实测当天时段正常派生可下单。**收尾=T5**(删废弃 cron 预生成代码:slot-roll/立即生成按钮/vercel cron 条目)。来源:BUG清单 B26。
- ✅ **B27 原生日期控件英文格式 mm/dd/yyyy(红线6)** —— **〔2026-06-10 对齐〕b-102 已完成**:统一中文日期控件(analytics/onsite/quota-rules 特例日历等),无英文格式残留。来源:BUG清单 B27。
- ✅ **B28 停车场地图占位文案误导** —— **〔2026-06-10 b-103 T1d 关单〕**road/parking 已换真高德整页地图组件;AmapContainer 四态硬约束「严禁永久加载中」(10s 超时+INVALID_USER_DOMAIN 兜底转 error,error 态带原因文案+重试按钮)。来源:BUG清单 B28。

- ✅ **B31 配额规则页:日历视图 + 规则层** —— **〔2026-06-10 对齐〕b-102 已完成**:月日历视图(可切表格)+ 派生惰性物化(B26)+ 每日总库存/单证单日上限规则层均落地实测。残留「立即生成未来时段」按钮废弃=**T5** 删除面。来源:BUG清单 B31。

- ✅ **B32 预约单查询无分页/全量渲染** —— **〔2026-06-10 对齐〕b-102 已完成**:服务端分页+分页器落地。附注的「本页无 Excel 导出」属运营体验缺口(非红线)仍开放,按需另立。来源:BUG清单 B32。

- 🛠️ **B33 自主回归批次(页5–15)** —— **〔2026-06-10 对齐〕①② 已由 b-102 完成**:① 行政图下钻 + 地理热力(heatmap/source 名实相符)✅;② channels/iot 设备/停车场 只读 CRUD 补齐 ✅。**残留**:③ 🔵 seed 资讯「免费不售票」票务词=**T6**;④ 🟡 road/parking 高德整页地图=**T1**。来源:BUG清单 B33。

- 🔵 **b-103 审计遗留微项(P2,低优)** —— ① 路况 `updatedAt` 在 fetch 缓存命中时仍标当前时刻(数据可旧达 60s),展示语义宜改「约 1 分钟内」;② `AmapContainer` markers 用 JSON 序列化做 effect 值比较,点位上百时每帧双份 O(n)(现 ≤12 无感)。来源:b-103 双视角审计。
- 🆕 **B37 Vercel 出口调高德 staticmap 502** —— 🔵 低(2026-06-10 线上实测)。`/api/screen/staticmap` 在 Vercel 上游返回非图片→代理 502;**同 key 同参数本机直调 200、且 traffic REST 在 Vercel 服务端正常**,疑高德静态地图服务对海外数据中心出口 IP 限制。影响仅 poster 专题一(已有 onError 占位兜底,不裂图)。备选:换国内出口代理/或 poster 改用 JSAPI 截帧。来源:b-103 线上验收。
- 🆕 **B35 miniprogram check:schema 存量红——shared-schema 与后端 booking schema 漂移** —— 🟡 中(2026-06-10 b-103 T2a 构建卡口时发现,stash 验证与 T2a 无关)。`pnpm check:schema` 报「端内缺少字段 date/name/startTime/endTime/各渠道quota + refine 文案不一致」:b-102(commit b8515cf)在后端 `booking/domain/schema.ts` 同文件新增时段类 schema/refine,检查脚本按整文件比对误报扩大;需要么收窄脚本比对面(只比 createBookingSchema 双要素字段),么同步 `miniprogram/shared-schema/booking.ts`。红线2 双要素字段本身(idCard/plateNumber/noVehicle)未漂移。来源:b-103 执行记录。
- ✅ **B16 会话滑动续期** —— **〔2026-06-10 b-103 T7 QA 关单〕**exp=120 docker 实测三场景全 PASS:① 续期不掉线(8 跳全 200、JWT exp 每请求后移 40s);② 停 GoTrue fail-closed(旧 access 有效期内 200→过期 307 /login+清两 cookie→恢复正常);③ ban 后 access 有效期内仍可访问(设计延迟≤exp)→过期续期被 GoTrue 拒→307 踢出。compose 已还原 604800。关单口径:「ban 生效延迟 ≤ GOTRUE_JWT_EXP」为设计行为,生产 exp 须 ≤3600(见§四)。来源:BUG清单 B16。
- ⛔ **B23 每日时段滚动生成(cron)** —— **〔2026-06-09 作废,口径并入 B26〕** 原方案(SysSlotTemplate + rollGenerateSlots cron 预物化)经设计评审判定单点脆弱(cron 漏跑=事故),改为 B26 的"规则派生 + 首单惰性物化、去 cron"。现有模板/特例日历数据模型可复用,生成/cron 逻辑废弃。
- 🧪 **B4 承载量阈值(红线4)** —— **已配置化(BE-A1)**:SysConfig + configService(承载量运营可改)+ CIRCUIT_BREAK_RATIO 常量统一。瞬时承载量正式数值仍**待 PRD**。来源:BUG清单 B4。
- 🧪 **B11 在园数实时刷新** —— "脏信号+回拉"无新代码,**待自托管 docker 实测**(BE-D 验证清单)。来源:BUG清单 B11。
- 🧪 **R2 SSE 鉴权 / Vercel 短路** —— **已加 503 短路(BE-D)**:Vercel/REALTIME=false → 503(非 500,不挂长连)。401/404 鉴权待自托管验证。来源:BUG清单 QA 矩阵。
- ✅ **B6 / R-存储:媒体上传(docker)** —— docker 侧**已跑通**(B29 补 S3 env 后 presign→PUT→public 读全通,CDP 实测封面预览成功)。Vercel 侧见四·存储(差 S3 key)。来源:R-存储/B29。
- ✅ **R-素材:封面选择器深集成** —— B25 已落:封面/编辑器均接 `/content/assets` 预签名直传 + 「从素材库选择」弹层。来源:R-素材/B25。
- ✅ **R-cfg:配额配置页重做** —— **〔2026-06-10 对齐〕随 B31+B26 由 b-102 完成**(规则派生 + 首单惰性物化 + 日历视图 + 总库存/单证上限),达 PRD 1.2 + 2026-06-04 日历决策。来源:BUG清单 B31。

## 二、视觉 —— 归"统一 theme pass"一次性做(设计决策)

> **〔2026-06-10 b-103 T3 theme-QA 结论〕** docker 生产构建 + CDP 截图矩阵 65 张(28 路由×浅/深 + 大屏 8 页 + 串台对照),归档 `UI/素材/theme-qa/`。**总体通过**:深色下 行政图/客流/日历/表格斑马/StatusChip/中文日期控件(B27 实锤已修)均正常;大屏↔后台双向串台无污染;road/parking 整页地图浮层双主题正常。**发现**:
> - 🆕 **B36 富文本编辑器工具栏深色白条** —— 🟡 中。`RichTextEditor.tsx` 硬编码浅 hex:工具栏 `bg-[#FAFAFA]`、激活态 `bg-[#E8F0E6]`、错误条 `bg-[#FEF2F2]`、分隔与吸顶语境,深色主题下成块白条(实锤 `theme-qa/content_news_new-dark.png`)。修法:换 bg-muted/bg-card 语义 token + 激活 bg-primary/10。
> - 🧪 **复检项(静态截图测不出,留交互 QA)**:① echarts 主题切换瞬间不重绘(结构性,切完刷新即正常);② 行政图 tooltip 深色样式;③ FOUC 硬刷新首帧;④ 原生 select/checkbox 的 color-scheme 声明。
> - 🔵 登录页深色左下角标语与底图对比度偏弱(可读,低优)。

> b-100 一次 theme pass 已落 FE-1/2/3/4(深浅双主题)。视觉/响应式/切换在 QA docker build 下定性。

- 🧪 **B18 登录页(FE-4)** —— 左右分栏 + 导览图背景(2.3MB→WebP 92.6KB 入 public)+ User/Lock 字段图标 + 全走 token + 深色遮罩。待 QA:LCP/分栏响应式/移动不下大图。
- 🧪 **B19 列表表格(FE-1/2)** —— 补全 14 个 shadcn `@theme` token(B19 真根因)+ Table 密度/斑马/对齐约定。
- 🧪 **B20 按钮样式(FE-2)** —— button 6 变体 hex→token + 清调用方内联 style。
- 🧪 **B24 侧边栏露白(FE-3)** —— 根去 h-full + aside flex stretch + bg-background。
- 🧪 **B14 菜单分组折叠(FE-3)** —— 分组可折叠 + **cookie 记忆**(SSR 无闪烁,优于 localStorage)+ 激活组强制展开 + a11y。另加 topbar 深色切换开关。
- 🛠️ **B9 内联 style 清理(FE-3)** —— base 组件 + 布尔三元/静态色转 token;剩 data-driven(图表/状态/进度宽)与 always-dark 大屏**按判定保留**。

### 不吃 token 的渲染面(theme 深浅切换需单独适配,T1/T2 已落地后按单复检)

> b-103 T3 沉淀:以下渲染面不走 `@theme` token,主题切换对它们无效,逐项有自己的适配机制:
> - **echarts 图表**:初始化时取色,切主题不重绘(结构性,见 theme-QA issue);`analytics/source` 行政图 `theme={null}`+硬编码色为已知必中点。
> - **原生表单控件**(date/select/checkbox):靠 `color-scheme: dark` 声明,缺省时深色下白控件。
> - **富文本内容区**(编辑器 ProseMirror 与 C 端 mp-html):内容样式独立(tag-style/内联),深色主题下白底正文属预期,B 端编辑器工具栏硬编码浅 hex 见 theme-QA issue。
> - **高德地图**(AmapContainer):自带 light/dark mapStyle 跟随 html.class(MutationObserver),已适配;但覆盖层/浮标色靠 token,大屏 always-dark 语境已单独定色。
> - **大屏 /screen**:always-dark 自有 `--screen-*` 体系,与后台主题隔离,双向串台已实测无污染。

## 三、阻塞 / 外部依赖

- 🧪 **R6 高德地图(=T1)** —— **〔2026-06-10 b-103 代码侧全部完成〕**:① REST 路况(rectangle+60s 缓存+诚实三态,假数据已删);② POI seed 8 点 GCJ-02(蒲江长秋山考证坐标,两库已灌);③ loader 单例+AmapContainer 四态共用组件;④ road/parking 整页地图+浮层(2026-06-04 决策落地);⑤ 大屏 situation/twin 真地图+poster 静态图(经 /api/screen/staticmap 服务端代理防 key 泄露)。**〔2026-06-10 带真 key 实测(从 Vercel env 经 PAT 同步到本地 .env)〕**:① REST 真路况 ✅(source=amap,返回过境蒲江的京昆高速双向 2 路段+真车速,矩形参数合法);② staticmap 真图 ✅(直调高德 200/PNG/153KB,画面即长秋山一带);③ JS 层加载链路 ✅(`window.AMap` 注入成功、地图容器创建、无鉴权/白名单错误——**白名单不填=全放行,非阻塞,原⛔解除**);④ **〔线上视觉验收已通过〕**真浏览器(CDP 驱动用户 Chrome)实测 road/parking/twin 三页真地图渲染:路况层着色、P 标记、twin 深色底图+8 POI 标点 fitView 正确(证据 `theme-qa/FIXED-*.jpg`)。过程中揪出并修复两个真 bug:容器高度塌 0(高德 SDK 强写 position:relative 致 inset-0 失效→改 h-full 显式定高)、首访冷加载超时过紧(10s→20s)。注:`app/.env` 被 dockerignore(防 key 进镜像),docker 栈地图仍走「Key 未配置」态;Vercel 构建期有 env,推送即生效。来源:R6 / BUG清单 U2。

## 四、临时部署善后(Vercel 演示)—— 纯运维,无代码(b-100 BE-E,须用户在外部面板执行)

> 审计 F1 拆两段:撤 token 与演示运行无关 → **立即**;改密码/删账号动运行态 → **演示后**。
- ✅ **【上线】Vercel 媒体直传存储——已通** —— **〔2026-06-10 完成〕** Supabase 建桶 `changqiushan-media`(public) + Vercel 配齐 8 个 `S3_*` env(含用户提供的 `S3_ACCESS_KEY`/`S3_SECRET_KEY`)+ CORS 验证 + Redeploy + CDP 实测线上封面上传成功(presign→Supabase Storage→commit→公网读 200)。docker 侧 B29 亦通。
- 🔒 **【演示后】轮换全部泄露凭据**(均在对话出现、视为已泄露):Supabase PAT · Vercel token · GitHub token · **Supabase S3 access key/secret**(面板 revoke 重建)· **高德 Web key + JS key + 安全密钥**(高德控制台重置)。轮换后同步 Vercel env + Redeploy。若曾进 git 历史须 filter-repo 清史 + 双 remote 强推。
- 🆕 **【演示后】改 Supabase DB 密码**(当前弱口令 `changqiushan`),**同步** Vercel env×2 / CI secret / 本地 .env → Redeploy;顺带轮换 GOTRUE_JWT_SECRET/CRON_SECRET。
- 🆕 **【演示后】删测试账号** `operator@changqiushan.demo`(删前确认留至少一个 SUPER_ADMIN)。
- 🔒 **【生产加固·安全审计】** ① MinIO S3 端口当前 `0.0.0.0:9000` 对外暴露 + 默认弱口令 → 生产改 `127.0.0.1` + 反代 HTTPS + 轮换 root 凭据(浏览器直传走反代/公网端点);② 生产 `GOTRUE_JWT_EXP` 必须从演示 7 天改回 ≤3600,靠 BE-C 滑动续期(否则 banAuthUser 后令牌仍有效至多 7 天);③ 多标签页并发续期竞态靠 GoTrue `REFRESH_TOKEN_REUSE_INTERVAL` 缓解(已知设计取舍)。来源:b-100 安全审计。
- 📌 最终生产:阿里云(ECS/容器 + RDS PG + OSS)+ ICP 备案域名(Vercel/Supabase 海外、无备案、微信支付回调收不到,仅适合临时演示)。来源:`b-deploy-vercel-supabase.md` §C2。

## 五、数字大屏(C1–C7,挂墙无登录)—— 已落代码,占位边界登记

> 代码已落(见 `.claude/plan/screen-数字大屏执行计划.md` 与对应 commit)。路由 `/screen/*`,middleware 整前缀放行 + 可选 `SCREEN_TOKEN` 软门;RSC 直读 repo 首屏 + `/api/screen/[metric]` 公开轮询(白名单+TTL+PII 白名单:只出聚合/计数/状态枚举);echarts 按需注册 + 等比缩放固定画布。运行时(docker)定性留 QA。

- 🧪 **大屏 C1–C7 全量** —— 7 页 + 地基(缩放外壳/图表库/公开端点/MV)已落。待 QA(docker 全栈):无登录可访问、缩放还原、轮询刷新、熔断闪红、Excel 导出、深色 token 隔离不污染后台。
- 🧪 **C4 7×24 热力 MV** —— `analytics_weekly_hourly_heat`(dow×hour)+ 注册 pg-boss/cron 刷新。待 QA:`pnpm db:migrate` 建 MV + 刷新生效。
- ⛔ **地图层(C1/C6/C7)** —— 中央 3D 地图 / 数字孪生底图 / GIS 基础 / 沉浸三维漫游,**阻塞高德 key(R6)**;现为占位组件 + 真实 `content.listPois` 坐标降级列表。key 到位仅换渲染层。
- 🆕 **地域画像(C1 客源地 TOP5 / C3 本外区县·市·省占比)** —— 需身份证→行政区划码表派生;现为占位标注。建议新增 `analytics.getVisitorRegionTop` + 码表。
- 🆕 **漏斗顶层「浏览量」(C2)** —— 需 C 端埋点;现漏斗从「预约→入园」起,顶层占位。
- 🆕 **天气 / AQI(C5)** —— 外部气象 API;现占位标注。
- 🆕 **POI 级阈值与实时在园计数 / 阈值动态档案 / 历史回放(C4)** —— 需 POI 级计数 + 阈值配置 + 快照表(pg-boss 二阶段);现占位。
- 🆕 **专项传感器读数(C6 闸机吞吐/路灯能耗、C7 闸机吞吐)/ 区域分布(C6)** —— 需对应 IoT 指标 / POI 级计数;现占位。
- 🆕 **散点第三维(C7 消费/客源 Z)** —— 现散点为「预约×入园」2 维;Z 维待接入。
- 🆕 **PNG「截图保存」导出(C5/C7)** —— echarts `getDataURL()` 客户端导出,需图表实例 ref 透出;本期未做,Excel 导出已就绪(C3/C4 公开 `/api/screen/export/[metric]`)。
- 📌 **生产加固** —— 大屏无登录:以 Nginx/反代 **IP 白名单**(大屏机固定内网 IP)为主 + 应用层 `SCREEN_TOKEN` 软门 + `/api/screen/*` 限频;PII 白名单铁律已在端点落地(绝不出 Booking/黑名单/申诉原始行)。

---

> 已全部完成的(b-98 C1–C10 / b-99 R1/R3/R4/R5/R7 / POC P1–P10 / 14 个 B 项)见各自计划与 BUG清单 QA 矩阵,本清单不重复。
