# 长秋山 B 端 · 未实现/遗留事项 单一汇总清单

> **唯一入口**:所有"还没做完"的事项收口到这一份,避免分散在多文档导致遗漏。
> 详情仍在原文档(本清单每项标注来源);**新发现的 issue 先登记到这里**,再按需展开到原文档。
> 状态:🆕 未做 · 🛠️ 部分(有未完成部分) · 🧪 待实测 · ⛔ 阻塞(外部依赖)
> 最后更新:2026-06-06(QA 全量回归后)。已关闭项见 `B端联调BUG清单.md` 的「QA 回归复核」矩阵。

---

## 一、功能 / 上线前(优先)

- 🛠️ **B16 会话滑动续期** —— 当前 cookie maxAge 已与 JWT 对齐,但**无滑动续期**,生产改回短时效后会每小时被踢。**生产前必做**。来源:`B端联调BUG清单.md` B16 / `b-99-收尾计划.md`。
- 🛠️ **B23 每日时段滚动生成(cron)** —— 手动建时段+今日有数据(止血已通过),但**结构性"按规则每日自动生成未来 N 天时段"未做**,seed 过期后当天仍会空窗。来源:BUG清单 B23 / `b-98-POC问题修复计划.md` C4(方案A)。
- 🛠️ **B4 承载量阈值(红线4)** —— 大屏 90% 闪红**机制已就绪**,但瞬时承载量是占位值,**待 PRD 给正式数值**才算闭合。来源:BUG清单 B4。
- 🧪 **B11 在园数实时刷新** —— Vercel 实时已关(数据快照);"脏信号+回拉"方案**待自托管 docker 环境实测**。来源:BUG清单 B11 / b-98 C3。
- 🧪/⚠️ **R2 SSE 鉴权** —— 鉴权代码在且 fail-closed,但 **Vercel 上 SSE 路由整体 500**(serverless 不兼容长连接);401 行为**待自托管验证**。来源:BUG清单 QA 矩阵 / `b-99-收尾计划.md` R2。
- 🆕 **B6 / R-存储:媒体上传** —— S3 兼容 driver(`@aws-sdk/client-s3`)+ 上传入口(封面等),三环境切 endpoint(MinIO/Supabase Storage/阿里云 OSS)。来源:`b-deploy-vercel-supabase.md` B6 / `B端PRD需求覆盖对照.md` R-存储。
- 🆕 **R-素材:活动素材归档入口** —— PRD 1.1 明列,活动运营管理缺该入口。来源:`B端PRD需求覆盖对照.md` R-素材。
- 🛠️ **R-cfg:配额配置页建时段** —— C4 止血加了手动建,完整"按日期类型建规则"未做。来源:PRD覆盖对照 R-cfg / b-98 C4。

## 二、视觉 —— 归"统一 theme pass"一次性做(设计决策)

> 之前定:纯视觉项不逐页改,统一收口到 theme(深浅双主题)最后一次性调。以下都在此范围。

- 🆕 **B18 登录页** —— 改左右分栏 + 导览图背景(素材 `UI/素材/登录页背景-导览图.jpg`,需转 WebP 放 `app/public/`)+ 前置图标 + 走 theme token。来源:BUG清单 B18。
- 🆕 **B19 列表表格** —— 补全 shadcn `@theme` token(muted/ring/input 等)+ 列宽/密度/斑马纹。来源:BUG清单 B19。
- 🆕 **B20 按钮样式** —— 主次层级、hover token、字号统一(去各页 `text-[12px]` 覆盖)。来源:BUG清单 B20。
- 🆕 **B24 侧边栏露白** —— 内容超屏时深绿背景不到底。来源:BUG清单 B24。
- 🆕 **B14 菜单分组折叠** —— accordion + 记忆展开态(localStorage)。来源:BUG清单 B14。
- 🛠️ **B9 内联 style 清理** —— 61→26,剩动态必需 + 部分可转 `cn()` 三元。来源:BUG清单 B9。

## 三、阻塞 / 外部依赖

- ⛔ **R6 高德地图** —— 实时路况/停车上图,**阻塞外部 key**(需 web key + 域名白名单);现为占位 + 数据表。来源:`b-99-收尾计划.md` R6 / BUG清单 U2。

## 四、临时部署善后(Vercel 演示)

- 🆕 **撤销三个 token**:Supabase PAT / Vercel token / GitHub PAT(均曾在对话出现)。
- 🆕 **改 Supabase DB 密码**(当前 `changqiushan`)。
- 🆕 **删测试账号** `operator@changqiushan.demo`(验 RBAC 时建)。
- 📌 最终生产:阿里云(ECS/容器 + RDS PG + OSS)+ ICP 备案域名(Vercel/Supabase 海外、无备案、微信支付回调收不到,仅适合临时演示)。来源:`b-deploy-vercel-supabase.md` §C2。

---

> 已全部完成的(b-98 C1–C10 / b-99 R1/R3/R4/R5/R7 / POC P1–P10 / 14 个 B 项)见各自计划与 BUG清单 QA 矩阵,本清单不重复。
