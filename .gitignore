node_modules/
logs/
UI_old_shots/
*.zip
.DS_Store
*.log
tmp/
.env

# 根目录的 Stitch 抓取脚本用 npm,pnpm-lock.yaml 是 pnpm 误跑生成的,不入库
/pnpm-lock.yaml

# swc 插件缓存(误生成)
.swc/

# 定时任务运行时锁(进程瞬时状态,含 pid/sessionId,不入库)
.claude/scheduled_tasks.lock
