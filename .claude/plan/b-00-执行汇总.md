# B 端后台 · 执行汇总（已更正：实际已基本交付）

> ⚠️ **本表此前一版严重失实**（基于源计划状态板 208–217 行的**附录 C 旧状态 2026-06-02**，把"后端就绪/前端空壳"当成现状）。
> 经核对**附录 D（第四轮 /multi-execute）/ 附录 E（第五轮独立复核 2026-06-03）+ 当前 main 代码实测**：
> B 端已实质交付——**0 个 `TODO 阶段`/`never[]=[]` 残留**，29 个 admin 页 24 页有真接线，导出红线5 达成，E1–E5 全修，3 个致命 500 修复，越权 RBAC 逐 action 加固，第五轮独立读码+实跑确认。
> 源计划权威结论见 [`b端后台分模块实施.md`](./b端后台分模块实施.md) 附录 **D/E**（非旧状态板）。

## ✅ 已交付并验证（附录 D/E 实跑核验，commit 全在 main）
- E1 导出权限门 / E2 角色入 JWT(`app_metadata.role`) / E3 B10 假成功 / E4 单日 TOCTOU 唯一索引 / E5 middleware issuer — 全修。
- 逐页接线全部完成：B10 补录·B22 查询+核销·B11 黑名单/申诉·B03/05/06/07 内容+B04 TipTap 富文本·B23 报名审核+B24 获奖·B14-19 分析·B20/21 IoT·B13 停车·B25 系统管理·导出红线5。
- 18 张表迁入库；3 个致命运行时 500（dotenv/instrumentation/dynamic ssr）修复；6 个 `actions.ts` + B08 内联 server action 全部 `requireRole` 加门。
- 三绿：`tsc --noEmit` 0 · `lint-cn` 0 · `eslint` 0 error（3 既有 warning）。
- 验收口径 1–5 全部经真实 HTTP + GoTrue 登录巡检确认。

## 📋 真实剩余收尾项（附录 E·三 / E·四，均为非阻断质量项）
| # | 项 | 状态 | 价值 | 备注 |
|---|---|---|---|---|
| R1 | **红线 domain 单测**（`assertDualElements`/`isCircuitBroken`/`isValidIdCard`/`canResume`/`withinDailyLimit`） | ❌ 全仓零测试 | 高(最廉价回归保险,纯函数易测) | 计划 2.9。需引入测试 runner(vitest) |
| R2 | **路由级 RBAC**（middleware 按角色拦路由） | ⚠️ 仅校 cookie | 中(纵深防御;action 层已是强制点) | 沿用 TODO |
| R3 | **a11y 走查**：`window.prompt`→内联表单、`<label htmlFor>` 关联、BarList `aria-label` | ❌ | 中 | `system/_account-manager.tsx` 等 |
| R4 | **X.2 设计系统英文黑名单断言测试** | ❌ | 低(lint-cn 已覆盖中文红线) | |
| R5 | **B12 高德路况地图**真实化 | ⚠️ 占位 | 低(依赖外部 key,非 npm) | 停车 B13 已附等价数据表(a11y) |
| R6 | `resetPassword`/`createAdmin` 审计日志补全；角色变更路径 | ⚠️ P2 | 低 | |

> 规模:R1 是唯一"高价值且该现在做"的——其余是纵深防御/外部依赖/低优先质量项。**不需要多智能体大计划**。
> 收尾计划见 [`b-99-收尾计划.md`](./b-99-收尾计划.md)。

## 状态结论
**B 端不存在"红线缺口/空壳未接线"**（那是旧状态板的误读）。可交付；剩 R1–R6 为质量收尾，按需推进。
