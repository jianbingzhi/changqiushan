# 长秋山 · 问题索引

> 每轮真机测试的 issue 文档放 `docs/issues/round-NN-<范围>.md`，本文件每轮登记一行。
>
> 状态纪律：实现方只标 **`fix`**（已修 + 自检通过）；**`pass` 只有独立验收方（测试）真机验证通过后才标**，且当场回填。一轮全部 `pass` 才在本索引标 ✅。
>
> 长期跟踪的「未实现 / 遗留事项」单一入口仍是 **`docs/待办清单.md`**；本索引只管**按轮次产生的测试 issue**。

## 轮次索引

| 轮次 | 日期 | 范围 / 环境 | 条目 | 状态 | 文档 |
|---|---|---|---|---|---|
| round-01 | 2026 年 7 月 28–29 日 | r1 演示服务器（`deploy-demo`）；**r3–r9 复验另起 `main` 本地 docker 全栈 + 真机 CDP** | 13（2 高 · 1 中偏高 · 4 中 · 6 低） | 🛠️ **8 条 `pass`**；**N13 🔴 回归待修**；余 N06 / N08 / N09 待人工裁决，N12 新增 | [round-01-演示服务器CDP验收.md](docs/issues/round-01-演示服务器CDP验收.md) |

## round-01 条目速览

| ID | 严重度 | 摘要 | 状态 |
|---|---|---|---|
| N01 | 🔴 高 | `/booking/onsite` 服务端渲染内容陈旧 6 天（日期 + 「当日暂无可预约时段」），hydration 失败并连带打掉该路由的深色主题 | ✔️ **pass**（r4 跨天 + r5 真机：深色跟随、console 0 错误、0 个 #418） |
| N06 | 🟠 中偏高 | `deploy-demo` 9 个提交从未合回 `main`，含 compose IPv4-only 真 bug 修复与 B104 超宽指挥总屏（**建议交人工定分支策略**） | 🛠️ 部分（compose 修复已进 `main` @ `6e67f18`；余 8 提交待定分支策略） |
| N02 | 🟡 中 | 富文本编辑器工具栏深色下整条白条（硬编码 `bg-[#FAFAFA]`，= 既有 B36） | ✔️ **pass**（r5：同口径全页扫描浅色块 1 → 0） |
| N03 | 🟡 中 | echarts 热力矩阵深色下底色恒白，**刷新也不恢复**（比既有文档描述更严重） | ✔️ **pass**（r5：像素采样 197→77，硬刷新仍 75，无浅色首帧） |
| N04 | 🔵 低 | 深色主题未声明 `color-scheme: dark`，原生 `select` 白底（与 N01 联动） | ✔️ **pass**（r3） |
| N05 | 🔵 低 | 来源分析行政图深色下为白色地块（已知设计取舍，留痕） | ✔️ **pass**（r5：地块亮度 15，tooltip 深色样式一并确认） |
| N07 | 🔵 低 | 路况「最后更新」恒等于当前时刻，实际数据可旧 60s | ✔️ **pass**（r3，三点采样全中） |
| N08 | 🔵 低 | 天气已接高德真实数据，但 `/screen/overview` 与待办清单 §五 仍标「待接入」，两屏口径不一致 | 🆕 阻塞于 N06（r3 核实：`main` 上 grep weather 0 命中，两屏都是占位、口径本就一致，修复方结论属实） |
| N10 | 🔵 低 | **新增（r5 撞见）** 热力图色阶条压在 X 轴刻度上，遮住「10时」「12时」；浅色深色均复现，非本轮修复引入 | ✔️ **pass**（r9：改右侧竖排，浅/深两主题真机各验一张，X 轴刻度恢复可读） |
| N11 | 🟡 中 | **新增（人工报，r5 核实）** 停车场 4 个坐标全为 NULL，`/traffic/parking` 地图零标点；「上图」功能从未被展示或验收过。根因 `prisma/seed.ts:334` 的 INSERT 无 `coordinates` 列；另 POI 表存在另一套带坐标、名称不同的停车场，需定正本 | ✔️ **pass**（r9：4 点全上图 + 状态配色/标签/位置栏均正确）⚠️ 命名正本仍待人工定 |
| **N13** | 🔴 **高** | **新增（r9）** 三块大屏（`/screen/heatmap`·`poster`·`command`）热力矩阵**完全空白**并抛 `splitArea` TypeError；canvas 非透明像素 0%。**由 `6d27afa` 引入的回归**（git -S + 父提交 A/B 双向印证，非 N10）；本验收方 r5 漏检自述见 issue 文档 | 🆕 |
| N12 | 🔵 低 | **新增（r9）** 停车场地图 2/4 标点被页面自身浮层卡片遮住；≤1440 宽必现、≥1920 宽不现 | 🆕 |
| N09 | 🟡 中 | **新增（r3 撞出）** compose 的 IPv6 开关与宿主出网能力强绑定：有公网 v6 的主机上 IPv4-only 会让容器连不上高德；无 v6 的主机上开 v6 又是黑洞 —— **怎么设都有一边坏** | 🆕 |

**待人工验证**：~~① 行政图 tooltip 深色样式~~ ✔️ r5 实测通过；~~② N01/N02/N03/N05 真机目视~~ ✔️ r5 全部 `pass`。

**r5 未竟**：深色回归扫描只跑完 4/10 路由（`/`、`/analytics/traffic`、`/analytics/profile`、`/booking/slots` 均干净）后 CDP 再次掉线，余 6 个路由未扫。

## 相关文档

- `docs/待办清单.md` —— 未实现 / 遗留事项单一入口
- `docs/B端联调BUG清单.md` —— 早期 B01–B40 联调 BUG 明细
- `PLAN.md` / `docs/plan/` —— 计划索引与执行计划
